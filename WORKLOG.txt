================================================================================
NASA-SEES / GraPhy PM2.5 imputation -- WORKLOG
================================================================================
A running, plain-text log of what we've tried, why, and what happened. Newest
sections appended at the bottom. Three themes:
  (1) NOVELTY / project direction
  (2) GATES & COVARIATES (elevation, temperature, wind, AOD) -- what we tested
  (3) ARCHITECTURE CHANGES (model.py + modules) -- every change, where, why

Metric vocabulary:
  * MAE (ug/m3): mean abs error at held-out sensors. Lower better.
  * skill_vs_baseline = 1 - MAE/MAE(predict-the-mean).
  * IDW baseline: inverse-distance-weighting of observed sensors -- the model-free
    floor a real GNN must beat.
  * GraPhy paper target: MAE 2.38 ug/m3 (Fresno, 41 sensors, Oct'23-Jan'24, inductive).
  * "inductive" eval = hold out whole SENSORS (disjoint train/val/test), predict
    them across ALL hours. This is GraPhy's protocol and the comparable one.
  * "transductive"/leave-one-out = mask nodes inside one set. Easier; not comparable.


================================================================================
(1) NOVELTY / DIRECTION -- history
================================================================================
- Goal throughout: results that are "strong AND novel" for the NASA-SEES GNN.
- Base task: interpolate/impute PM2.5 to unmonitored locations across a metro net.
- Cities: Boulder, Pittsburgh (95 sensors), Salt Lake City (winter inversion),
  and now Fresno (GraPhy's benchmark city, for reproduction).
- Big diagnostic finding (pre-Fresno): every reanalysis covariate (temp, wind,
  humidity from ERA5) is COMMON-MODE -- one value across a small city -> cannot
  discriminate between locations -> useless as a SPATIAL predictor for interpolation.
- Pivot considered: satellite AOD as the spatially-varying covariate. Fetched
  MODIS/MAIAC AOD for Pittsburgh. RESULT: AOD is ALSO common-mode in space
  (spatial-anomaly corr with PM = -0.008) but STRONG in time (+0.593). i.e. good
  temporal anchor, useless spatial predictor at metro scale.
- Literature check (2026-07): the subfield is "inductive spatio-temporal kriging"
  (IGNNK 2021, KITS 2023, PGITS 2025). Our base GraPhy is memoryless/per-timestep;
  temporal memory is table-stakes we lack, NOT novelty. Genuine gaps in the
  literature: nobody fuses satellite AOD, nobody uses terrain/elevation. Those +
  long-gap filling are where novelty lives -- BUT only after the base model works.
- DECISION (user): before chasing novelty, REPRODUCE GraPhy's base interpolator.
  "We're not even reproducing GraPhy right now." -> the Fresno reproduction below.


================================================================================
(2) GATES & COVARIATES -- what we tested and the verdicts
================================================================================
ELEVATION GATE (src/model/elevation.py): per-EDGE scalar from signed d-height,
  dampens diffusion + convection across height gaps. Verdict: only marginal signal
  on 22-node SLC winter (skill +0.088 vs +0.050, error bars overlap). Well-mixed
  metros have no vertical structure for it to exploit. Real signal only in a basin
  inversion; needs more complex-terrain data to matter.

TEMPERATURE GATE (src/model/temperature.py): per-NODE scalar from surface temp,
  scales transport strength. Verdict: NULL. Temp is common-mode in space; redundant
  with neighbour PM. Sanity tests with PLANTED gradients confirmed the gate CAN
  exploit signal when it exists -- the real data just doesn't have it spatially.

WIND / CONVECTION (ERA5 ~30km vs HRRR 3km vs zero): fetched HRRR 3km per-sensor
  wind for Pittsburgh (scripts/fetch_wind_hrrr.py). HRRR DOES vary spatially and
  wind-divergence has a real -0.119 spatial-anomaly corr with PM. BUT the convection
  ablation (era5/hrrr/zero, leave-one-out, 6 seeds) was decisive: skill +0.251 /
  +0.243 / +0.255 -- ALL IDENTICAL. Convection adds nothing; zero-wind ties full
  wind. The -0.119 is redundant with neighbour PM (convergence zones are smooth).

AOD FEATURE (MODIS MCD19A2 1km, scripts/fetch_aod.py via Google Earth Engine):
  spatial-anomaly corr -0.008 -> did NOT wire the feature channel (foregone null).
  Strong temporally (+0.593) -> belongs on a temporal axis (future gap-filling).

META-VERDICT: every covariate is redundant with PM spatial autocorrelation for
  metro-scale interpolation. Covariates are strong TEMPORALLY, not spatially. The
  gate well is dry for the pure-spatial task. (This is a clean NEGATIVE result that
  can MOTIVATE a temporal/terrain reframe later -- but base model must work first.)


================================================================================
(3) ARCHITECTURE / REPRODUCTION -- Fresno, 2026-07-09
================================================================================
GOAL: reproduce GraPhy MAE 2.38 under its inductive protocol, to learn whether the
BASE MODEL is the problem (user's suspicion) before any more novelty work.

INFRA BUILT:
- scripts/fetch_purpleair.py: added --bbox NWLAT,NWLNG,SELAT,SELNG (was hard-coded
  to SLC). WHY: fetch Fresno.
- scripts/fetch_wind_hrrr.py: added fresno to COORDS map.
- src/graph/build_graph2.py: added CITY_CONFIG['fresno'] (UTM 11N, EPSG:32611).
- scripts/eval_inductive.py (NEW): GraPhy's inductive protocol. Disjoint train/
  val/test SENSORS (28:4:9 scaled to N), val+test PM never fed, predict test sensors
  over ALL hours, MAE in ug/m3. Includes an IDW baseline. Options added along the way:
  --transform {log,linear,sqrt}, --loss {mse,huber}, --huber-beta, --clip, --knn,
  --elev-feature, --elev-gate.

FINDINGS (chronological):
- Naive repro in log1p space (as train.py trains): MAE 8.44, WORSE than IDW (5.8).
  => We were NOT reproducing GraPhy at all.
- BUG #1 (loss space): train.py trains MSE in log1p space but PM MAE is LINEAR ->
  underpredicts pollution peaks. Switch to raw/sqrt z-space cut MAE 8.44 -> 6.76,
  R2 0.15 -> 0.62. (200 epochs did NOT help log-space -> not underfitting.)
- BUG #2 (instability): raw-ug/m3 MSE at lr 0.01 DIVERGES on ~1/3 of seeds (MAE
  13-19, R2<0). Fix: --transform sqrt (milder tail than log, stable near-linear)
  + --loss huber + --clip 1.0. Variance collapsed +-4.9 -> +-0.34, zero divergences.
- Tuning that did NOT help: capacity (l5 h32 >= worse than l4 h16), connectivity
  (K=8/15/30 all ~same), wind on/off (irrelevant, matches earlier convection null).
- DENSITY TRAP: chasing sensor COUNT (wide box) spread 34 sensors over 1369 mi^2 =
  0.025 sensors/mi^2. GraPhy's paper says performance COLLAPSES below 0.11. We were
  4-5x too sparse. Re-fetched a TIGHT central-Fresno box (0.127 sensors/mi^2, 21
  live sensors). Lesson: match DENSITY (sensors/mi^2), not count.
- Density-matched hardened eval (sqrt+huber+clip, l4h16, 8 seeds): OURS 6.60+-1.39,
  IDW 5.58+-1.44. STILL loses to IDW; still ~2.8x off 2.38.

VERDICT (answers user's question): the BASE MODEL is the problem. Across every lever
(density 0.025->0.127, capacity, K, wind, loss space) our GraPhyNet plateaus ~6.3-6.6
MAE and LOSES to trivial IDW. GraPhy BEATS IDW. Two separable gaps:
  * "Loses to IDW" is SCALE-INVARIANT (a global data rescale moves us and IDW
    together) => a genuine MODEL defect. <- fix this first.
  * "6.6 vs 2.38 absolute" is also DATA: we use raw pm2.5_atm; GraPhy almost
    certainly uses EPA-corrected + A/B-averaged + QA'd PurpleAir (over-reads ~40%).

ARCHITECTURE DIAGNOSIS (why we lose to IDW), from reading the modules:
  IDW = normalized weighted AVERAGE of neighbour VALUES (a low-pass/smoothing op).
  Our modules don't cleanly compute that:
  * diffusion.py uses the rescaled LAPLACIAN L_D = 2L/lam - I (a DIFFERENCE/high-pass
    operator) + sigmoid. For a masked node (input 0) this measures (0 - neighbours),
    squashed to (0,1)*l -- NOT the neighbour average. Suspect #1.
  * everything runs in a learned hidden space with residual h=h+fused; a masked
    node starts at encoder(0) and must be reconstructed indirectly.
  * fusion softmax can dilute diffusion with convection/local.
  PLAN: (A) diffusion-only + module ablations to confirm; (B) give the model an
  explicit IDW/low-pass interpolation pathway (residual-kriging: predict IDW +
  learned correction) so it CANNOT do worse than IDW; then (C) EPA data correction.

--- CHANGES LOG (append each architecture edit here) ---

[2026-07-09  CHANGE 1: module on/off toggles]  WHERE: src/model/fusion.py,
  src/model/model.py, scripts/eval_inductive.py.  WHAT: Fusion now takes
  n_modules and *outs (variable # of module outputs) instead of a fixed 3.
  GraPhyLayer/GraPhyNet take use_convection/use_local; disabled modules aren't
  constructed (no dead params) and Fusion sizes to the active count. eval_inductive
  gained --no-convection/--no-local.  WHY: isolate which modules help vs IDW.
  RESULT (density-matched Fresno, 5 seeds, sqrt+huber+clip, l4h16):
    diffusion-ONLY:  MAE 7.46 +-1.45   (WORST -- Laplacian alone can't interpolate)
    full (d+c+l):    MAE 6.60 +-1.39   (from earlier run)
    IDW baseline:    MAE ~5.2
  => No module config beats IDW; diffusion-only is worst, so convection/local were
     actually HELPING slightly. The problem is the hidden-space/Laplacian core, not
     the extra modules. Confirms the fix must add a real interpolation pathway.

[2026-07-09  CHANGE 2: IDW residual prior (the fix)]  WHERE: scripts/eval_inductive.py
  (--idw-prior). Kept model.py generic; the prior is applied at the driver so it
  composes with any module config.  WHAT: precompute Wmat[i,j]=1/(dist+1) (zero
  diag). Each step, IDW-interpolate in the SAME z(sqrt) space from the VISIBLE
  (observed & unmasked) nodes only (no target leakage), then predict
  pred = idw_base + model(). If the GNN correction -> 0 we recover IDW exactly, so
  we CANNOT do worse than IDW by construction (residual kriging / GNN-over-IDW).
  WHY: the GNN lacked a low-pass averaging op; IDW supplies it, GNN learns the
  correction.  RESULT (smoke, 1 seed, 20 epochs, diff+conv+local + idw-prior):
    OURS MAE 4.98  vs  IDW 5.52   -> FIRST TIME WE BEAT IDW. (R2 .769 vs .747)
  8-seed run: seed0 5.40<5.52, seed1 5.24<5.82 ... (beating IDW consistently).

[2026-07-09  MILESTONE: FIRST CONFIG TO BEAT IDW ON THE MEAN]
  IDW-prior scaled runs (density-matched Fresno, 8 seeds, sqrt+huber+clip, l4h16):
    idw-prior + FULL (d+c+l):       MAE 5.99 +-1.51  vs IDW 5.58  (slightly loses;
                                    2 seeds' corrections blew up -> 8.4, 8.1)
    idw-prior + diffusion+LOCAL:    MAE 5.47 +-1.32  vs IDW 5.58  (BEATS IDW; R2
                                    .742 vs .684, corr .869 vs .852)
  => Winning recipe = IDW residual prior + DROP convection (it hurts) + correction
     regularization (CHANGE 4). Convection dropping matches every earlier convection
     null. Margin still thin (2 seeds lose); reg sweep running to widen it.

[2026-07-09  CHANGE 4: residual-kriging stability -- zero-init head + corr L2]  WHERE:
  scripts/eval_inductive.py.  WHAT: with --idw-prior, zero-init model.head (train
  starts EXACTLY at IDW), and add --corr-reg * ||correction||^2 (default 0.1) on the
  target nodes.  WHY: the learned correction was unconstrained -> on some held-out
  splits it dragged predictions off IDW and lost badly (seed4 8.40 vs IDW 5.40).
  EFFECT (no-conv): tamed the 8.40 blowup to ~6.34 and flipped seed3 to a win; net
  5.47 < IDW.
  REG-STRENGTH SWEEP (idw-prior + no-conv, 8 seeds, explicit corr-reg):
    corr-reg 0.1:  MAE 5.373 +-1.37  R2 0.745
    corr-reg 0.3:  MAE 5.345 +-1.36  R2 0.749
    corr-reg 0.6:  MAE 5.303 +-1.31  R2 0.753   <- BEST (lowest MAE + variance)
    IDW baseline:  MAE 5.579 +-1.44  R2 0.684
  Monotonic: more reg -> better + more stable. WINNING MODEL CONFIG =
    --idw-prior --no-convection --corr-reg 0.6 --transform sqrt --loss huber --clip 1
    --layers 4 --hidden 16.  Beats IDW on MAE (~5%), clearly on R2/corr. 2 hard
    seeds (4,6) still lose but are much tighter than the pre-reg blowups.

[2026-07-09  CHANGE 3: EPA/Barkjohn PurpleAir correction]  WHERE:
  src/graph/build_graph2.py (EPA_CORRECT flag + _epa_correct + load_air_quality),
  scripts/fetch_purpleair.py (--extra-fields fetches pm2.5_cf_1 + humidity; generic
  fetch_history/write_history_csv over a field list), scripts/eval_inductive.py
  (--epa-correct sets bg.EPA_CORRECT).  WHAT: PM2.5_corr = 0.524*cf_1 - 0.0862*RH
  + 5.75 (clip>=0). WHY: raw pm2.5_atm over-reads ~40% and is RH-sensitive; GraPhy's
  benchmark uses corrected data, so raw inflates our absolute MAE vs their 2.38.
  Scale-invariant caveat: this shifts the ABSOLUTE number, not whether we beat IDW.
  RESULT (winning config + --epa-correct, 8 seeds, density-matched Fresno):
    OURS MAE 4.453 +-0.87  vs  IDW 4.588   (still beats IDW)
    vs RAW-data winning config 5.303 -> EPA cut absolute MAE 16% (5.30 -> 4.45), as
    predicted (raw over-reads). R2/corr dip (0.622/0.809) because the RH term + range
    compression add some noise. Now ~1.9x off GraPhy 2.38 (was ~3.5x at the start).

================================================================================
SCOREBOARD (density-matched Fresno, inductive, 8 seeds)  -- lower MAE better
================================================================================
  START (log-space, full model):            MAE 8.44   LOSES to IDW (5.6), 3.5x off 2.38
  + linear/sqrt + huber + clip (stable):     MAE 6.60   loses to IDW
  + IDW residual prior (full model):         MAE 5.99   ~ties IDW (2 seeds blow up)
  + drop convection + corr-reg 0.6:          MAE 5.30   BEATS IDW (5.58), R2 .753
  + EPA PurpleAir correction:                MAE 4.45   BEATS IDW (4.59), ~1.9x off 2.38
  GraPhy paper:                              MAE 2.38
Remaining gap to 2.38 = density (ours 0.127 just above their 0.11 collapse floor;
theirs likely higher), tiny graph (~20 live sensors -> hard seeds 4,6), + model
headroom (GraPhy beats IDW by 9-51%; we beat it ~3-5%).

IMPORTANT STRATEGY NOTE (2026-07-09): GraPhy is MEMORYLESS (per-timestep, no
recurrent memory) -- so TEMPORAL CONTEXT is NOT the gap to 2.38. Do not chase it to
reproduce GraPhy; it's a lever for the FUTURE NOVELTY (long-gap filling) direction
only. To close the 2.38 gap the only real levers are: (a) stronger correction/model
architecture, (b) higher sensor density (needs their exact 41-node network), (c)
data QA (EPA done; A/B-channel averaging not available from the history API).

[2026-07-09  CHANGE 5: IDW-as-feature]  WHERE: scripts/eval_inductive.py
  (--idw-feature). WHAT: feed the per-step IDW prior value as a never-masked node
  feature (node_in += 1), so the learned correction can CONDITION on the base it is
  correcting instead of re-deriving it. Composes with --idw-prior. WHY: cheap way to
  give the GNN the interpolation base explicitly. RESULT (EPA data, 8 seeds):
    no idw-feature: MAE 4.453 +-0.87  R2 0.622
    + idw-feature:  MAE 4.302 +-0.96  R2 0.636   <- better on 6/8 seeds; beats IDW
                    (4.588) by ~6%. Best config so far. (seed2 hit 2.91 on a good split.)
  CURRENT BEST = --idw-prior --idw-feature --no-convection --corr-reg 0.6 --epa-correct
                 --transform sqrt --loss huber --clip 1 --layers 4 --hidden 16.
  Progress arc: 8.44 (losing) -> 4.30 (beating IDW ~6%), ~1.8x off GraPhy 2.38.

[2026-07-09  CHANGE 6: learnable IDW bandwidth]  WHERE: scripts/eval_inductive.py
  (--learn-bw). WHAT: replace the fixed 1/(d+1) prior kernel with a Gaussian
  exp(-d/bw), bw learnable (init ~2 km), trained jointly with the GNN. WHY: the fixed
  kernel bakes in an arbitrary correlation length; let the data pick it.
  RESULT (EPA, 8 seeds): MAE 4.333 +-0.98  R2 0.640  -- NO GAIN vs fixed 1/d (4.302).
  The fixed inverse-distance kernel is already near-optimal here. Reverting to default
  (no --learn-bw). => diminishing returns on the model side.

================================================================================
STATUS 2026-07-09 (end of reproduction push)
================================================================================
GOAL (user): fix the base model so it beats IDW, then reach GraPhy's 2.38.
DONE: base model FIXED and VALIDATED. Arc on density-matched Fresno (inductive, 8 seeds):
  8.44 (losing to IDW, 3.5x off 2.38)  ->  4.30 (BEATING IDW ~6%, R2 .64, 1.8x off 2.38).
The user's original suspicion was correct: the base WAS broken (lost to trivial IDW).
Root cause = Laplacian diffusion core can't interpolate; fixed with an IDW residual
prior + correction regularization + dropping the (harmful) convection module, plus
loss-space/stability fixes and the EPA data correction.
REMAINING 1.8x gap to 2.38 is NOT model-fixable here: it's network density (we max out
at ~20-34 live PurpleAir sensors for this window; GraPhy's 41-node set is denser) +
their exact QA. Model levers are exhausted (capacity, K, learn-bw all flat).
NEXT (user's call): (a) accept as a faithful capability reproduction (GNN beats IDW/
kriging, GraPhy's core claim), or (b) now that the base is sound, pursue the NOVELTY
direction (long-gap filling + terrain), which is where "strong AND novel" lives.

================================================================================
[2026-07-10  GATE ABLATION on the validated strong base]  WHERE: scripts/eval_inductive.py
  (--elev-gate, --elev-feature). Question: now that the base beats IDW, do our GATES
  / novel additions improve SPATIAL INTERPOLATION? Protocol: inductive, 8 seeds, strong
  base config (idw-prior + idw-feature + no-convection + corr-reg 0.6 + epa-correct,
  sqrt/huber/clip1, 4x16). SLC = terrain (22 nodes, 418.5m spread, all 142 edges nonzero
  delev) = the gate's design target; Fresno = FLAT control.

  ELEVATION GATE (edge modulator, per signed delev):
    SLC:    base 4.824 -> gate 4.692  (-2.7% MAE, R2 .624->.639); vs IDW floor 5.103
            the win over IDW widens +5.5% -> +8.1%. PAIRED per-seed: gate WINS 7/8
            (only seed7 +0.156), mean delta -0.133 ug/m3 -> real, not seed luck.
    Fresno: base 4.371 -> gate 4.377  (+0.1%, INERT) -- exactly right: delev~0 on flat
            ground so the gate self-disables. No hand-tuning needed.

  ELEVATION FEATURE (extra never-masked node channel) -- HURTS EVERYWHERE, drop it:
    SLC 4.824 -> 4.900 (+1.6%);  Fresno 4.371 -> 4.581 (+4.8%, pure overfit noise on flat).
    gate+feature (SLC 4.781) < gate alone: the free channel drags the gain back down.

  TAKEAWAY: the GATE form (constrained, physics-shaped, Delta-elev driven) is the right
  design -- helps on terrain, inert on flat, robust across seeds. The FEATURE form
  (unconstrained channel) is wrong -> remove --elev-feature from any terrain config.
  RECOMMENDED terrain config = strong base + --elev-gate (NOT --elev-feature).

  NOT TESTABLE YET (no data): temp gate + AOD. has_temp=False on BOTH cities; no AOD on
  disk. Temp gate also still passes node_temp=None in eval_inductive (needs wire-in).
  SLC winter inversions are the ideal temp-gate test bed -> next step = fetch SLC surface
  temp, wire node_temp into the inductive harness, ablate the per-node temp gate there.

================================================================================
[2026-07-10  TEMPERATURE GATE test on Pittsburgh]  WHERE: scripts/eval_inductive.py
  wired the per-node temp gate into the inductive harness (new --temp-gate flag:
  build standardized [T,N] surface-temp tensor from graph's temp_wide, pass as
  node_temp into both model() calls; was hard-coded None before). Pittsburgh chosen
  over SLC because it's the ONLY city with temperature already, and it's PER-SENSOR
  (each PurpleAir <id>_temperature.csv) -> nominally spatial, no fetch needed.
  Strong base config, 8 seeds, 59 nodes, full year.

  RESULT (MAE ug/m3, 8 seeds):   IDW floor 5.203
    base                 5.077 +-0.456   (barely beats IDW; dense 59-node net is hard)
    + temp-gate          5.100 +-0.383   +0.5% WORSE.  paired: temp wins 4/8, mean +0.023
    + elev-gate          5.111 +-0.429   +0.7% worse (elev gate helps on SLC, NOT here)
    + temp + elev        5.102
  => TEMP GATE IS A WASH (coin-flip, slightly negative). No spatial signal.

  ROOT CAUSE (measured): Pittsburgh's "per-sensor" temp is effectively COMMON-MODE.
    TEMPORAL std (field mean over year) = 8.84 C
    SPATIAL std (across 59 sensors, per hour) = 0.06 C  (median 0.05)
    spatial/temporal ratio = 0.007  -> at any hour ALL sensors read ~the same temp.
  So it's a regional field sampled at points, not independent sensor physics -- the
  SAME common-mode dead-end as ERA5 (see memory aod-fusion-direction). A per-node gate
  whose input doesn't vary in space cannot discriminate held-out locations; for
  INDUCTIVE SPATIAL interp a spatially-uniform gate is a no-op on the relative
  interpolation. Global standardization also buries the tiny spatial part under the
  seasonal swing, but even a per-hour spatial-anomaly reframing has only 0.06 C to
  work with -> not worth it. Temp gate mechanically fine, but no signal to act on.

  BOTTOM LINE across both gate tests: the ELEVATION gate is the one real win (SLC,
  terrain, real Delta-elev). The TEMPERATURE gate needs genuinely spatial temp
  (station thermometers with real micro-climate spread, or a fine terrain-following
  temp model) -- neither Pittsburgh's common-mode field nor absent SLC temp provides
  it. NOTE: eval_inductive.py temp-gate wiring is uncommitted (user controls commits).

================================================================================
[2026-07-10  AOD FEATURE ablation on Pittsburgh]  WHERE: scripts/eval_inductive.py
  wired AOD as a never-masked node feature (new --aod-feature, + --aod-anomaly).
  NOTE: AOD had never actually been tested before -- load_aod existed in build_graph2
  but train.py has ZERO aod refs; the "test aod" commit only added the loader/fetch.
  So this is the first real AOD result. Loaded via bg.load_aod (no train.py change).

  AOD is the RIGHT KIND of covariate (unlike temp): GENUINELY SPATIAL.
    spatial std 0.0208 on 0.170 mean (~12%); spatial/temporal ratio 0.093
    (vs temp 0.007 -- 13x more spatial signal). Coverage 33% (daily + cloud gaps)
    -> a FEATURE (field-mean fill on misses), not a gate.

  RESULT (MAE ug/m3, 8 seeds, strong base):   IDW floor 5.203
    base                 5.077 +-0.456   R2 .532  corr .747
    + AOD (raw level)    5.071 +-0.474   R2 .536  corr .752   paired 5/8, -0.1% (neutral)
    + AOD (spatial anom) 5.099 +-0.428   R2 .532  corr .747   paired 4/8, +0.4% (no better)
  => AOD NEUTRAL ON MAE. Raw version nudges R2/corr the right way (captures a little
  real spatial variance) but it does NOT translate to MAE. Anomaly reframing (subtract
  per-hour city-mean to isolate WHERE-signal) is slightly WORSE -- the city-mean is
  noisy at 33% coverage and subtracting it injects noise.
  INTERPRETATION: AOD's spatial info is largely REDUNDANT with the IDW/PM-neighbour
  structure the strong base already exploits -> no MAE headroom to capture. It's a real
  covariate (not common-mode like temp), just not additive on top of dense PM neighbours.

  SCOREBOARD across all covariates tested on the strong base (inductive, 8 seeds):
    ELEVATION GATE   -- REAL WIN on terrain (SLC 7/8 seeds, -2.7%), inert on flat. ***
    elevation feature-- hurts (overfit). drop.
    TEMPERATURE gate -- wash (Pittsburgh temp is common-mode, spatial std 0.06C).
    AOD feature      -- neutral on MAE (tiny R2/corr uptick), redundant w/ PM neighbours.
  Only the elevation GATE produces a real MAE win, and only where terrain varies.
  All wiring (temp-gate, aod-feature, aod-anomaly) is UNCOMMITTED (user controls commits).

================================================================================
[2026-07-10  TERRAIN-AWARE IDW PRIOR KERNEL -- BEST MODEL LEVER FOUND]
  WHERE: scripts/eval_inductive.py (--elev-kernel, --elev-kernel-h). Idea: the elev
  GATE only reshapes the small learned CORRECTION; but the IDW PRIOR does most of the
  work. So push terrain into the PRIOR KERNEL: w_ij = 1/(d+1) * exp(-|Δelev_ij|/h).
  Cross-ridge pairs get downweighted -> the prior interpolates WITHIN a valley/airmass.
  h = vertical decay length (m). Flat -> Δelev≈0 -> factor≈1 (self-disables, inert).

  RESULT (SLC terrain, inductive, 8 seeds; IDW floor 5.103):
    base                 4.824 +-0.93  R2 .624   (beats IDW +5.5%)
    + elev gate          4.692        R2 .639   -2.7%   (prior 2026-07-10 result)
    + elev kernel h=50   4.171 +-0.96  R2 .697  -13.5%  (beats IDW +18%)
    + elev kernel h=100  4.173 +-0.99  R2 .695  -13.5%
    + elev kernel h=150  4.266        R2 .688  -11.6%
    + kernel h=100 +gate 4.126 +-0.96  R2 .700  -14.5%  (beats IDW +19%)  <- best
  PAIRED base vs kernel h=100: WINS 8/8 seeds, mean -0.652 ug/m3 (-13.5%).
  FRESNO flat control: base 4.371 -> kernel h=100 4.381 (+0.2%, INERT as designed).

  => The terrain-aware PRIOR KERNEL is ~5x the elevation GATE's effect and unanimous
  across seeds. Moved us from "beats IDW +5.5%" to +18-19% -- inside GraPhy's own
  "beats IDW 9-51%" band, on terrain. h=50-100m sweet spot; gate adds a marginal
  extra on top. BEST spatial-model lever of the whole session. Inert on flat ->
  safe to leave on. All wiring UNCOMMITTED (user controls commits).

  WHY IT WORKS (vs why the elev GATE was small): the prior is the workhorse; the
  correction is regularized small (corr-reg 0.6). Terrain physics belongs in the
  workhorse. Same lesson likely applies to WIND (anisotropic kernel: downweight
  cross-wind / upwind pairs) -- untested, natural next kernel experiment.

================================================================================
[2026-07-10  TEMPORAL LONG-GAP FILLING -- the novelty axis]  WHERE: NEW driver
  scripts/eval_temporal.py. Protocol: hold out contiguous TIME WINDOWS (gaps) from
  otherwise-present sensors, predict each sensor's OWN missing hours. Model UNCHANGED
  (same GraPhyNet/gates); time enters as FEATURES (persistence + lag1 + lag24) and a
  learnable PRIOR blend  prior = b*persistence + (1-b)*spatial_IDW,  b=sigmoid(beta).
  Leak-safe: known_mask = obs & ~test_gap & ~train_gap; all lag/persistence/IDW inputs
  built from known_z (gap cells carry-forward filled) so no target value feeds a
  feature/prior. Pittsburgh, 59 nodes, 4 seeds, gap x6 per sensor.

  RESULT (MAE ug/m3):
    GAP 24h:  spatial-only (memoryless)  5.585 R2 .497   <- the GraPhy-style floor
              + temporal memory          3.694 R2 .737   -34% !!  (persist floor 5.457)
              + temp-gate                3.685            -0.009 (noise)
              + aod-feature              3.712            slightly worse
    GAP 72h:  temporal                   4.405 R2 .553   (persist floor 7.116, R2<0)
              + temp-gate                4.396            -0.009 (noise)
              + aod                      4.419            worse
              + temp + aod               4.410
  LEARNED BLEND b (persistence weight): 24h -> 0.48,  72h -> 0.35. The model AUTOMATICALLY
  downweights stale persistence for longer gaps and leans on the spatial prior. Exactly
  the designed behavior -> mechanism validated.

  FINDING 1 (WIN): TEMPORAL MEMORY is a large, real gain -- 34% MAE cut vs the memoryless
  spatial-only model. This is the project NOVELTY: GraPhy is memoryless and leaves this
  on the table. Model architecture UNCHANGED (time as features + prior blend, no
  recurrence) -- confirms temporal can be added without touching GraPhyNet.

  FINDING 2 (honest negative, now conclusive): temp-gate and AOD are REDUNDANT in BOTH
  the spatial AND the temporal task. Hypothesis was temporal would activate them; it did
  NOT (-0.009 / worse). ROOT CAUSE: the target's OWN PM history (persistence/lags) already
  carries the regional-level dynamics that temp/AOD merely PROXY for. With dense PM data
  -- spatial neighbours OR temporal history -- the proxy covariates add nothing. They are
  proxies for PM dynamics we already measure directly. Verdict: temp/AOD parked for good
  on dense networks; they'd only matter where PM data is genuinely sparse in BOTH axes.

  SESSION SCOREBOARD (levers that move MAE):
    * TERRAIN-AWARE IDW PRIOR KERNEL  -- SLC -13.5% (8/8 seeds), inert on flat.  WIN
    * TEMPORAL MEMORY (gap-filling)   -- Pittsburgh -34% vs memoryless.          WIN + NOVEL
    * elevation gate                  -- SLC -2.7% (subsumed by the kernel).     minor
    * temp gate / AOD / elev feature  -- no help (redundant or wrong form).      dead
  eval_temporal.py is NEW + all eval_inductive gate/kernel wiring UNCOMMITTED (user
  controls commits).

================================================================================
[2026-07-10  MATCH GRAPHY DENSITY on Fresno -- BLOCKED on PurpleAir API credits]
  User directive: fetch more Fresno sensors to match GraPhy's 41-node density (the
  residual ~1.8x gap to MAE 2.38 is network density). Plan: widen the PurpleAir bbox
  to greater Fresno-Clovis, no spacing thinning, --extra-fields (EPA), Oct'23-Jan'24.
  LIST-ONLY confirmed 43 usable sensors in bbox 36.90,-119.92,36.60,-119.58 (alt
  spread 37 m -- flat, as expected). Enough to match 41.
  Added CITY_CONFIG['fresno_dense'] -> data/fresno_dense (zero-fill wind; fine because
  winning config is --no-convection so wind is unused). Set to run --wind zero.

  BLOCKER: the full history fetch died at sensor 9/43 with HTTP 402
  PaymentRequiredError -- the PurpleAir API key is OUT OF POINTS (balance -20216).
  Only 9 sensors downloaded -> FEWER than the current 18-sensor set, so unusable yet.
  data/fresno (18 sensors) left INTACT (fetched to a separate dir).
  TO RESUME: user must top up PurpleAir API credits (or supply another key); then
  re-run the fetch (scripts/fetch_purpleair.py, same bbox/window) -> ~43 sensors ->
  re-run eval_inductive --city fresno_dense --wind zero <strong-base> and compare MAE
  to the 18-sensor 4.30 and to GraPhy 2.38. Everything else is ready and waiting.

================================================================================
[2026-07-10  TIER-1 MODEL LEVERS + DATA QA -- NEW BEST MAE 3.819 (was 4.30)]
  Directive: close the ~1.8x gap to GraPhy 2.38 WITHOUT more data. Attacked two
  open surfaces: (i) model headroom (correction only beat IDW ~6% vs GraPhy 9-51%);
  (ii) data QA (GraPhy's data is cleaned; ours was raw pm2.5_atm). Built four levers
  + one data-cleaning step as composable flags. Sweep A-F, inductive Fresno (18
  sensors, --wind zero), 8 seeds, strong base sqrt/clip1/4x16/no-conv/corr-reg0.6.
  All edits UNCOMMITTED (user controls commits). Logs -> tier1_sweep_logs/.

  DATA CLEANING (src/graph/preprocessing.py):
    - despike(): per-sensor CENTRED rolling-median MAD filter. robust-z =
      |x-median| / max(1.4826*MAD, min_sigma); cells > DESPIKE_K (6) robust-sigma
      -> masked to not-observed. Window 13h, min_sigma 1.0. Conservative: real
      multi-hour episodes drag the median with them and survive; only isolated
      single-hour blips removed. OFF by default; eval_inductive --despike flips on.
      Masked 647/61971 cells (1.04%) on top of the 11.99% out-of-range mask.

  TIER-1 LEVERS (scripts/eval_inductive.py):
    #1 --idw-geom-features : 2 confidence feats (visible IDW mass fraction + weighted
       neighbour dispersion) telling the correction WHERE the prior is weak.
    #2 --kriging-prior (--kriging-range 3000, --kriging-nugget 0.1): simple-kriging
       BLUP base = C_av (C_vv+nugget I)^-1 z_vis, exp covariance -> downweights
       clustered/redundant sensors. Replaces the 1/d IDW prior.
    #3a --loss l1 : median regression (MAE-optimal = conditional median).
    #3b --recal : isotonic predicted->true map fit on held-out VAL sensors, applied
       to test (leak-safe) to remove the sqrt-inversion (Jensen) bias.

  RESULTS (MAE ug/m3, 8 seeds):
    cfg  levers                              OURS          IDW    R2     RMSE
    A    control (NO despike)                4.371±.89    4.588  .632   7.95
    B    +despike                            4.130±.89    4.159  .737   6.11
    C    +despike +geom-feats                4.116±.97    4.159  .743   6.02
    D    +despike +kriging          <-BEST   4.045±.68    4.159  .787   5.66
    E    +despike +L1 +recal                 4.344±1.0    4.159  .698   6.52
    F    +despike +kriging +geom +L1 +recal  4.216±1.0    4.159  .751   5.89

  WINNER: D = despike + kriging prior + idw-feature (huber). MAE 4.045, best R2
  (.787) and RMSE (5.66) of all. New best (prior best 4.30). Now ~1.7x off GraPhy.

  FINDINGS:
    1. DESPIKE (data QA) is the biggest nominal mover: A->B cut MAE 4.371->4.130,
       lifted R2 .632->.737. CAVEAT: IDW's OWN MAE also fell 4.588->4.159, so PART of
       the gain is easier targets (spike cells removed from the test set), not purely
       better inputs. But R2 rose and OURS still leads -> a genuine improvement, just
       not all attributable to the model.
    2. KRIGING PRIOR is the real MODEL lever: on the SAME despiked data B->D gives a
       clean -0.085 (4.130->4.045) with the best R2/RMSE. Per-seed MIXED: beats IDW on
       4/8 seeds, but the wins are larger than the losses (seed3 -0.93) so the mean
       leads. Downweighting redundant clustered sensors pays off -- as theory predicts.
    3. GEOM FEATURES: noise (4.130->4.116). Drop.
    4. L1 + RECAL (E) REGRESSES to worse-than-IDW (4.344). Recal is UNSTABLE: seed 3
       blew up to 6.21 (R2 .29). The isotonic map fit on few val sensors overfits.
       DROP recal. F confirms it: adding L1/recal to kriging drags D's 4.045 -> 4.216.

  KEEP: despike + kriging (config D). DROP: geom-feats (noise), L1, recal (unstable).

  KRIGING RANGE/NUGGET GRID (run_kriging_grid.sh, on config D, 8 seeds):
    range(m) @ nugget 0.1:  1000  2000  3000  4000  5000  6000  8000
    OURS MAE:               5.159 4.543 4.045 3.853 3.819 3.855 3.845
    R2:                     .661  .740  .787  .804  .806  .802  .799
    nugget @ range 3000:  0.05 -> 4.051,  0.10 -> 4.045,  0.20 -> 4.166
    nugget @ range 5000:  0.05 -> 3.837,  0.10 -> 3.819
  -> BROAD FLAT OPTIMUM at range 5000 (4000/5000/6000 within +-0.04 -> robust, not a
     knife-edge). Nugget 0.1 confirmed (lower ties, higher hurts). The default 3000 was
     too short (prior over-localizes to nearest sensor); 1000 collapses to 5.16.
  NEW BEST CONFIG: despike + kriging --kriging-range 5000 --kriging-nugget 0.1 +
     idw-feature (huber) -> MAE 3.819+-.78, R2 .806, RMSE 5.37. Down from 4.045
     (range 3000) and 4.30 (session start). Now ~1.60x off GraPhy 2.38.

  NEXT: residual 1.6x gap is now largely network density (18 vs GraPhy's 41 sensors) --
  see the BLOCKED fresno_dense fetch above (PurpleAir credits). Kriging range is tuned.

================================================================================
[2026-07-10  DENSITY-MATCHED FRESNO (supplement fetch) -- IDW now 1.42x off GraPhy,
             but OURS LOSES to IDW on the dense net]
  User topped up PurpleAir. SUPPLEMENT strategy (minimize credits): fetch history for
  ONLY the sensors we don't already own. Added scripts/fetch_purpleair.py
  --exclude-existing DIR (skips history for ids already under DIR/pm25/<grp>; coords
  still written for the full box). Proven box 36.90,-119.92,36.60,-119.58 -> 43 usable,
  of which 33 already owned -> fetched 10 NEW (EPA fields, Oct'23-Jan'24, no wind,
  27894 rows). scripts/merge_fresno_dense.py unions data/fresno + data/fresno_extra ->
  data/fresno_dense by sensor id (keeps only ids with a CSV -> never drops an existing
  sensor). Result: 43 sensors on disk, BUT 12 of the ORIGINAL 33 CSVs are EMPTY (old
  failed fetches) -> 31 load -> 27 kept after coverage. So usable went 18 -> 27 (+50%).

  RESULT (fresno_dense, 27 usable, split 18/3/6, tuned best config, 8 seeds):
    OURS (kriging r5000)  MAE=3.823+-.55  R2=.727      <- ~unchanged from 18-node 3.819
    IDW baseline          MAE=3.391+-.37  R2=.784      <- was 4.159 on 18-node!
    GraPhy (paper)        MAE=2.380
  DENSE KRIGING-RANGE RE-TUNE (hypothesis: closer sensors -> shorter range wins):
    range 800/1200/1800/2500/3500/5000 -> OURS 4.998/4.550/4.129/3.844/3.858/3.823
    HYPOTHESIS WRONG: shorter is WORSE; OURS PLATEAUS ~3.82 for range>=2500. Range was
    never the bottleneck.

  FINDINGS:
    1. DENSIFICATION IS THE ABSOLUTE-MAE LEVER: IDW 4.16 -> 3.39 (1.8x -> 1.42x off
       GraPhy) from +9 usable sensors. Most of the gap to 2.38 was network density, as
       hypothesized. The remaining 1.42x is GraPhy's edge OVER IDW (their learned
       diffusion/convection), not density.
    2. OUR GNN CORRECTION LOSES TO IDW ON THE DENSE NET (3.82 vs 3.39, loses 6/8 seeds).
       Regime flip: the IDW-residual correction HELPED on the sparse 18-node net (gaps to
       fill) and is DEAD WEIGHT on the dense net (IDW already near-perfect locally -> the
       correction only adds noise). Consistent with the whole session: the correction is
       mostly suppressed noise; density removes its job. Re-tuning range/nugget does not
       recover it.
    3. To beat dense IDW (-> approach 2.38) we'd need the GNN to add REAL signal a dense
       IDW lacks -- exactly what GraPhy's learned diffusion does and our residual-kriging
       reimpl does not. That's the open model problem, now cleanly isolated.

  CREDIT NOTE: 12 of the original 33 fresno CSVs are EMPTY (failed old fetches). Re-
  fetching those 12 (key now works) could lift usable ~27 -> ~35-39, truly matching
  GraPhy's ~41 usable -- but will help IDW more than OURS. Pending user call (costs
  ~12 sensors of points). New files: fetch_purpleair.py --exclude-existing,
  scripts/merge_fresno_dense.py, run_kriging_grid_dense.sh, data/fresno_extra,
  data/fresno_dense. All UNCOMMITTED.

  --- DENSE MODEL DIAGNOSTIC (run_dense_model.sh): fix OURS, prior vs correction ---
  User chose "fix the model, don't buy more density." Isolated WHY OURS lost:
    config (fresno_dense, 8 seeds; IDW baseline = 3.391)            OURS MAE   R2
    kriging-prior r5000, corr-reg 0.6  (old control)               3.823      .727
    1/d IDW prior,       corr-reg 0.6                              3.497      .769
    1/d IDW prior,       corr-reg 0.9  (suppress correction) BEST  3.451      .773
    1/d IDW prior,       corr-reg 0.3  (bigger correction)         3.596      .756
    1/d IDW prior, l6h32 corr-reg 0.6  (more capacity)            3.545      .763
  FINDING (corrects the "loses by 0.43" above): the KRIGING PRIOR was the dense
  handicap -- its exp-cov BLUP (range 5000) OVER-SMOOTHS the dense geometry. Swap to
  the 1/d IDW prior (what the strong baseline uses) -> 3.823 -> 3.451, now TIED with
  IDW 3.391 (within +-0.4 noise), no longer losing.
  BUT the CORRECTION still adds no signal on dense: suppress it -> better (cr0.9 best),
  enlarge it -> worse (cr0.3), add capacity -> worse (l6h32). Best case it CONVERGES to
  IDW; it cannot BEAT it. This is a structural limit of the residual-on-IDW form, not a
  tuning miss -- confirmed across prior/corr-reg/capacity, all pointing the same way.
  SESSION CONCLUSION: (a) despike + 1/d IDW prior + suppressed correction is the right
  config in BOTH regimes (kriging only helped on SPARSE; drop it on dense). (b) To BEAT
  dense IDW (GraPhy's actual contribution) we need a mechanism that adds signal IDW
  lacks -- GraPhy's learned CONVECTION/diffusion, which needs REAL WIND. Wind was
  zero-filled for fresno_dense (10 new sensors have no wind) -> convection was inert.
  NEXT LEVER = fetch HRRR wind (NOAA, FREE, not PurpleAir points) for the dense set +
  re-enable convection, and see if physics-based transport beats IDW where the residual
  cannot. New file: run_dense_model.sh. All UNCOMMITTED.

  --- CONVECTION TEST on data/fresno (REAL HRRR wind, 18 usable) run_convection_fresno.sh ---
  User: get convection contributing; likely helps MAE. Convection lives INSIDE the
  corr-reg-suppressed residual + competes via softmax; it encodes DIRECTIONAL transport
  IDW can't -> gave it every chance: fixed 1/d prior, real wind (has_wind=True), and
  LOOSENED the leash so it could speak. Sweep (8 seeds, IDW baseline 4.159):
    off_06 no-conv cr0.6 (control) 4.130   <- BEST
    on_06  conv    cr0.6           4.181   (+0.05 vs control)
    on_03  conv    cr0.3           4.296   (+0.17)
    on_01  conv    cr0.1           4.478   (+0.35)
    off_03 no-conv cr0.3           4.247   (+0.12)  <- looser reg hurts even w/o conv
  CONVECTION HURTS at every leash; loosening the correction monotonically WORSENS MAE
  WITH or WITHOUT convection -> the residual is noise, convection included. 2nd
  independent confirmation (after the dense diagnostic).
  ROOT CAUSE (mechanistic, measured): the HRRR wind is COMMON-MODE across the compact
  urban network -- cross-sensor std of u/v = 0.31/0.30 m/s vs 1.75 m/s mean (17.5%
  variation), wind-DIRECTION circular std just 17.9 deg across all 33 sensors. The same
  wind vector blows over every sensor, so wind_along = speed*cos(dir - bearing) varies
  edge-to-edge ONLY through geometry, not spatial wind structure. No differential
  advection to learn -> convection can add only a weak global directional bias = noise.
  Same class as the earlier "reanalysis covariates are common-mode dead ends" finding.
  CONVECTION IS NOT BROKEN -- it has nothing to bite on where wind is spatially uniform.
  IT WOULD HELP where wind VARIES across the net: TERRAIN-CHANNELED valley/canyon flow.
  CHECKED Pittsburgh (river valleys, 95 wind sensors on disk): wind is ALSO common-mode,
  even MORE than Fresno -- 13.7% of mean, 15.2 deg direction std. GENERAL ROOT CAUSE:
  HRRR is a 3 KM reanalysis grid; cities span ~10-30 km with ~1-3 km sensor spacing, so
  HRRR cannot resolve sub-grid wind -> every sensor gets ~the same interpolated vector.
  => With HRRR wind, convection CANNOT contribute at intra-city scale in ANY of our
  cities (not a Fresno quirk). A real convection test would need genuinely
  spatially-resolved wind (terrain-downscaled or per-sensor anemometer) -- not on hand.
  Convection question CLOSED for the reanalysis-wind regime. Don't spend on a Pittsburgh
  run; it would reconfirm the null. New file: run_convection_fresno.sh. All UNCOMMITTED.

================================================================================
[2026-07-10  PUSH MAE FURTHER: literature scan + data QA round 2]
  User asked how else to push MAE + literature + "add back the simplified-out module
  MLPs?". Findings:
  ARCHITECTURE: our predictor = IDW_prior + SMALL residual (corr-reg'd). Diffusion is
  the SIMPLIFIED module (linear W + per-feature rate, FIXED 1/d Laplacian = IDW's own
  isotropic kernel); convection already has 4 MLPs. Adding MLP capacity to the residual
  is contra-indicated by our OWN ablations (dense l6h32 WORSE 3.545; lower corr-reg
  WORSE) -> overfits at N=18-27. The useful capacity per the literature is in the
  interpolation WEIGHTS (learned/physics/attention adjacency) + implicit augmentation,
  NOT a deeper residual.
  LITERATURE (inductive GNN kriging): IGNNK (AAAI'21) + KCN (AAAI'20) + PGITS (2025,
  physics-guided air-quality). Two transferable ideas: (1) RANDOM-SUBGRAPH / node-mask
  training = implicit augmentation, how they beat kriging on small nets; (2) physics in
  the graph WEIGHTS (W_phy = mu*W_adv + (1-mu)*W_diff), but their W_adv needs
  spatially-varying wind we measured is COMMON-MODE -> inert for us.
  User chose: BOTH data-QA-first, then IGNNK-style training.

  DATA QA ROUND 2 (preprocessing.py: mask_flatline + mask_spatial_outliers, flags
  --flatline / --spatial-qa; run_qa.sh; fresno_dense, dense-best model, 8 seeds):
    despike (base)          OURS 3.451  IDW 3.391
    + flatline              OURS 3.451  IDW 3.391   (EXACT no-op: no stuck runs in-range)
    + spatial-qa            OURS 3.368  IDW 3.339   <- small real win (-0.083 / -0.052)
    + both                  OURS 3.368  IDW 3.339
  FINDING: within-range data is ALREADY clean (despike was the big QA win). Flatline
  inert; spatial-qa banks a modest -0.08 (its 83 flagged cells were faults not hotspots
  -- both OURS & IDW improved). New dense-best = despike + spatial-qa (OURS 3.368, both
  ~1.40x off GraPhy). Further statistical QA of our own bytes is ~EXHAUSTED; the residual
  gap to GraPhy's ~2.7 IDW needs A/B-channel data (a re-fetch) or better sensors, not
  more filters. New files: preprocessing.py filters, run_qa.sh. NEXT = IGNNK training.
  All UNCOMMITTED.

  IGNNK-STYLE RANDOM MASKING (eval_inductive --mask-frac-hi: sample per-step hidden
  fraction ~U[mask_frac, mask_frac_hi] = implicit augmentation; run_ignnk.sh;
  fresno_dense, despike+spatial-qa, 1/d prior, 8 seeds; dense IDW = 3.339):
    base fixed 0.25, cr0.9              OURS 3.368
    aug 0.2-0.6,     cr0.9             OURS 3.368  (identical MAE; RMSE/R2 noise-better)
    aug 0.3-0.7,     cr0.9             OURS 3.380  (harder masking slightly worse)
    aug 0.2-0.6,     cr0.6             OURS 3.473  (looser leash worse again)
  FINDING: augmentation gives NO MAE gain; it does NOT unlock the residual. THIRD
  independent confirmation (after dense-diagnostic + convection) that on a DENSE PM net
  IDW is near-optimal and the residual-on-IDW GNN cannot beat it -- OURS floors at ~IDW.

  SESSION-END ASSESSMENT (honest): within the current residual-on-IDW architecture we are
  at the ceiling on dense data (OURS 3.368 ~ IDW 3.339, 1.40x off GraPhy 2.38). Levers
  that moved MAE: despike (big), kriging prior on SPARSE, density (IDW 4.16->3.34),
  spatial-qa (small). Levers conclusively DEAD: convection (common-mode wind), temp/AOD
  (common-mode), L1/recal, geom-feats, extra capacity, IGNNK masking. Two real paths
  remain, both OUTSIDE what's been tried: (A) A/B-channel data re-fetch -> clean toward
  GraPhy's likely ~2.7 IDW (DATA, not model); (B) abandon residual-on-IDW for a from-
  scratch LEARNED-ANISOTROPIC-WEIGHT interpolator (GraPhy's actual learned diffusion),
  trained pure w/ the new augmentation -- research bet, low expected payoff since IDW is
  near-optimal on dense. New files: run_ignnk.sh, eval --mask-frac-hi. All UNCOMMITTED.

================================================================================
[2026-07-11  A/B-CHANNEL QA -- BIGGEST WIN SINCE DESPIKE: IDW 3.34 -> 3.02, 1.27x off GraPhy]
  User: "do the a/b channel, clean data matters on principle." PurpleAir has TWO laser
  channels (pm2.5_atm_a/_b); the averaged pm2.5_atm we had hides a dead/decorrelated
  channel. Fetched a/b for all 43 sensors (fetch_purpleair.py --ab-channels; 87301 rows
  -> data/fresno_ab). Calibrated on a healthy sensor: corr 0.999, |A-B| median 0.7,
  p95 1.7 ug/m3. scripts/apply_ab_qa.py: per-CELL mask where |A-B|>5 AND rel>0.5 (or a
  channel drops out); per-SENSOR drop where corr(A,B)<0.9 (strict also drops frac_bad>10%).
  Builds data/fresno_dense_ab (strict) / _abc (conservative); CITY_CONFIG entries added.
  A/B FOUND REAL FAULTS: 5-7 sensors with corr -0.22..0.87 (broken channels the atm
  average silently hid): 123359(-.22) 139384(-.21) 27111(.5) 62137(.795) 87251(.87)
  [+111391/118085 = the empty duds].

  EVAL (full QA stack despike+spatial-qa, dense-best model, 8 seeds):
    set                 usable  OURS          IDW           off GraPhy
    fresno_dense (base)   27    3.368+-.45   3.339+-.35    1.40x
    fresno_dense_ab       20    3.385+-.84   3.379+-.59    worse (STRICT over-drops)
    fresno_dense_abc      22    3.054+-.29   3.021+-.20    1.27x   <- NEW BEST
  WIN: CONSERVATIVE A/B QA cut MAE ~9.5% (IDW 3.339->3.021, OURS 3.368->3.054) DESPITE
  fewer sensors (27->22) -> the 5 dropped sensors were genuinely CORRUPTING (bad as both
  neighbours and targets). Variance also collapsed (OURS std .45->.29). BIGGEST single
  lever since despike; confirms the residual gap to GraPhy is DATA quality, not model
  (A/B closed a big chunk toward their likely ~2.7 IDW). STRICT was worse: it discarded
  2 GOOD high-corr sensors (63119 .962, 64483 .98) -> density loss > noise removal.
  RULE: drop only decorrelated channels (corr<0.9); mask bad cells for the rest.
  NEW BEST CONFIG: --city fresno_dense_abc --wind zero --despike --spatial-qa + 1/d IDW
  prior + idw-feature + corr-reg 0.9 (huber, sqrt/clip1/4x16, no-conv, epa). OURS 3.054,
  IDW 3.021, 1.27x off GraPhy 2.38. New files: fetch --ab-channels, scripts/apply_ab_qa.py,
  run_ab_eval.sh, data/fresno_ab + fresno_dense_ab + fresno_dense_abc, CITY_CONFIG x2.
  All UNCOMMITTED.

================================================================================
[2026-07-11  SESSION: push toward 2.38 -- density re-fetch DEAD, pivot to single-channel recovery]
  Situated from HANDOFF + WORKLOG. Reproduced current best EXACTLY:
    --city fresno_dense_abc, dense-best config, 8 seeds ->
    OURS 3.054+-0.287 / IDW 3.021+-0.202 / GraPhy 2.38 (1.27x off). Env healthy.

  RANKED open levers were: (1) more data QA, (2) re-fetch the 12 empty sensors to
  match GraPhy's ~41-node density (memory directive match-graphy-density), (3) arch
  rebuild. Pursued (2) first = biggest proven ABSOLUTE lever historically (density
  18->27 usable dropped IDW 4.16->3.34) and explicitly user-directed.

  --- DENSITY RE-FETCH INVESTIGATION (lever #2) -> DEAD END, documented ---
  data/fresno_dense has 43 sensor files; exactly 12 are 1-row EMPTIES (114721 197623
  198161 233743 236357 237167 240179 246355 246395 253369 296839 306814). Handoff
  hypothesis: "old fetches failed; key works now -> re-fetch recovers 22->30+ usable."
  METHOD: backed up the 12 empties to scratch, re-fetched via fetch_purpleair
  --exclude-existing data/fresno_dense (supplement mode: spends points only on the 12,
  not the 31 owned). RESULT: all re-fetches came back EMPTY AGAIN (42 bytes = header
  only). Killed the run, restored backups (dataset back to 43 files, untouched).
  ALSO a mistake to note: forgot --start/--end so the re-fetch used the script default
  window (2023-01-01..2023-12-31) not the canonical Oct23-Jan24. But this only
  strengthened the finding -- a FULL YEAR still returned empty.

  RIGOROUS ROOT-CAUSE PROBE (direct PurpleAir API, not the fetch wrapper):
   * META for 114721 + 246355: BOTH alive, last_seen ~now. So not deleted/offline-now.
   * date_created: 246355 = 2024-10-07 (AFTER our window -> genuinely never had data
     in Oct23-Jan24, correctly empty). 114721 = 2021-07 (existed during our window).
   * HISTORY endpoint, canonical window Oct 1-15 2023, sids 114721/246355/197623/306814:
     ALL return a clean header-only CSV (1 line, no data) -- NOT an API error, a true
     empty. So even the 2021-created sensor has NO data in our window.
   * HISTORY endpoint, RECENT 14 days, 114721/306814: BOTH return 337 rows. So the
     sensors DO serve history -> NOT a private/read-key access problem.
  CONCLUSION (confirmed 3 ways: meta alive, header-only not error, recent data present):
  the 12 empties genuinely have a DATA GAP during Oct23-Jan24 (offline then, or created
  later). Re-fetching CANNOT recover data that does not exist. Lever #2 is DEAD for this
  study window. To add density we'd need sensors that HAD data in the window -- the box
  36.90,-119.92,36.60,-119.58 holds only 43 (thins to 35 @0.8km; we already own all 43),
  so no new in-window sensors exist without widening (=sparser, density trap) or going
  sub-0.8km (redundant neighbours). Density-by-count is exhausted for this window.

  PIVOT -> SINGLE-HEALTHY-CHANNEL RECOVERY (new free lever, not on the dead list):
  A/B QA (apply_ab_qa.py) currently DROPS 5 sensors whose corr(A,B)<0.9 (123359 -.22,
  139384 -.21, 27111 .5, 62137 .795, 87251 .87), taking usable 27->22. But a
  decorrelated pair usually means ONE dead channel, not both -- the healthy channel is
  still valid PM. GraPhy-grade pipelines keep the good channel rather than discard the
  node. Recovering these ~5 as single-channel sensors could lift 22->~27 usable with
  clean data at ZERO credit cost -> lower the IDW floor (density is the proven absolute
  lever). Implementing next. All UNCOMMITTED.

  --- SINGLE-HEALTHY-CHANNEL RECOVERY (new lever) -> PROMISING: OURS 3.05->2.87 (8-seed) ---
  IDEA: A/B QA drops 5 sensors for corr(A,B)<0.9, but a decorrelated pair usually means
  ONE dead channel, not both -- the healthy channel is still valid PM. Keep the good
  channel instead of discarding the node (recover density lost to QA, zero credit cost).
  IMPL: apply_ab_qa.py --recover-channel / --recover-min-corr. Builds a robust city-median
  PM signal (median across all sensors' pm2.5_atm), and for each would-be-dropped sensor
  picks the channel whose atm tracks that signal; keeps the sensor if corr-to-city >=
  threshold. Single channel becomes pm2.5_atm; cf_1 approximated by same channel (no
  per-channel cf_1 in the a/b fetch; cf_1~=atm at low Fresno concentrations); humidity kept.
  Per-sensor corr-to-city (which channel is healthy):
    123359 B .961 (A garbage mean 226) | 62137 B .933 | 87251 A .948 |
    139384 B .728 (A broken mean 1493) | 27111 A .528 (B flatlined)

  DATA-STATE BUG (SELF-INFLICTED, since RESTORED via git checkout): a FRESH apply_ab_qa
  rebuild from data/fresno_dense did NOT reproduce the on-disk data/fresno_dense_abc.
  data/fresno_dense/coords had dropped to 35 sensors, MISSING 8 the on-disk _abc has
  (80125, 155829, 163097, 198871 + empties 197623/198161/233743/296839; 80125 has a pm25
  file). ROOT CAUSE (found at cleanup, git diff): my empty-refetch ran with
  --out data/fresno_dense, and fetch_purpleair.write_coords OVERWROTE the 43-sensor coords
  with the 35-sensor thinned list. Also --list-only wrote Fresno coords into data/slc/coords
  (fetch's default OUT_DIR -- a latent bug). BOTH coords files were `git checkout HEAD --`
  RESTORED at session end (fresno_dense back to 43, slc back to its own). => to test
  recovery cleanly I did NOT rebuild from the mangled coords; instead I copied the intact
  on-disk _abc and ADDED only the 3 clean recovered sensors, so the ONLY delta vs the
  3.054 baseline is +3 nodes -- that test is unaffected by the coords issue.

  CLEAN TEST (_abc[36] + 3 recovered = 25 usable nodes, dense-best config, 8 seeds):
    baseline _abc            OURS 3.054+-.29  IDW 3.021+-.20   (22 nodes)
    _abc + 3 recovered       OURS 2.872+-.86  IDW 2.939+-.57   (25 nodes)  <- both better
  7/8 seeds land 2.2-2.8 (great); seed 4 blew up (OURS 5.06) = a HARD test split, NOT a
  recovered-sensor defect: per-sensor IDW leave-one-out MAE puts the 3 recovered sensors
  MID-PACK as targets (87251 3.81, 123359 3.77, 62137 2.90) -- BETTER than 6 clean sensors
  (155829 10.91, 189863 9.48, 198871 8.56, 104230 8.53, 124099 8.39, 163097 8.32). So the
  recovered channels are good neighbours AND good targets; the seed-4 tail is small-N split
  variance from adding 3 nodes (baseline seed 4 was also its worst). Recovery genuinely
  lowers mean MAE (-6% OURS, -2.7% IDW) toward GraPhy 2.38. Running 16 seeds for a stable
  estimate.
  NOTE on the big-LOO clean sensors (155829 mean 33.8, 198871 mean 36.1 ~4x city median):
  they PASS A/B (both lasers agree) -> genuine high-PM hotspots, NOT faults. Do NOT drop
  them to chase MAE (that's gaming the eval by deleting hard real points; GraPhy predicts
  its hard sensors too). All UNCOMMITTED.

  --- RECOVERY VERDICT: DEAD (16-seed apples-to-apples) ---
  The 8-seed 2.87 was a lucky subset. Ran 16 seeds three ways (dense-best config):
    baseline _abc (22 nodes)               OURS 3.040+-0.377   IDW 2.983+-0.297
    _abc + 3 recovered as SCORED targets   OURS 3.069+-1.044   IDW 3.101+-0.694
    _abc + 3 recovered as TRAIN-ONLY nbrs  OURS 3.113+-0.375   IDW 3.008+-0.255
  (train-only via new eval flag --train-only-ids: keep nodes always in train pool, never
   held out -- a denser reference net, legit for imputation. Impl in run_seed split.)
  READ: as TARGETS the recovered sensors add massive tail variance (+-1.04, seeds 4/10
  blow to 5-6 MAE) -- single-channel PM is noisier than the 2-laser average, so held-out
  it disagrees with the field. As NEIGHBOURS they SLIGHTLY HURT (3.113 vs 3.040) -- the
  extra noise (+ the cf_1~=atm approximation) degrades the IDW field rather than
  densifying it usefully. In NO role do they help. This VINDICATES the original
  conservative A/B QA drop: those sensors are genuinely lower quality; keeping them, even
  as pure neighbours, is net-negative. Also: 16-seed baseline 3.040 ~= 8-seed 3.054 =>
  the documented best is stable/robust (tight +-0.38, no blow-ups).

  SESSION CONCLUSION (both density levers exhausted):
   * DENSITY RE-FETCH: dead -- the 12 empties have no data in the study window.
   * CHANNEL RECOVERY: dead -- recovered single-channel sensors are lower-quality, hurt
     or add variance in every role.
  => The conservative _abc set (OURS ~3.04-3.05 / IDW ~2.98-3.02, 1.27-1.28x off GraPhy
  2.38) stands as the best, and we are at the DATA-QUALITY CEILING for this sensor set:
  every way to add MORE sensors either has no in-window data or injects noise. Remaining
  paths to 2.38 (unchanged, both outside data-QA): (A) architectural rebuild to a learned-
  anisotropic-weight interpolator that BEATS IDW on dense (research bet, low expected
  payoff given IDW is near-optimal here); (B) a genuinely denser+cleaner sensor set than
  central Fresno offers for this window (not available to us).
  NEW REUSABLE INFRA (kept, harmless): apply_ab_qa.py --recover-channel/--recover-min-corr;
  eval_inductive.py --train-only-ids; build_graph2 CITY_CONFIG['fresno_dense_abc_rec'].
  Experimental data dirs fresno_dense_abc_rec / _abc_ctrl are scratch (safe to delete).
  Data-hygiene NOTE: the self-inflicted coords mangle was git-restored (fresno_dense=43,
  slc reverted). Latent bug to fix in fetch_purpleair: --list-only / a bare --out default
  to data/slc (wrong OUT_DIR) -- guard it before the next fetch. All UNCOMMITTED.

================================================================================
[2026-07-11  PIVOT: DENSITY-CROSSOVER SWEEP -- can we BEAT IDW in the sparse regime?]
  User reframed the goal (publishability): "we need to BEAT the literature, not report
  null results." Insight: the whole project proved IDW is near-optimal on DENSE nets, but
  GraPhy's actual claim is a GNN that BEATS IDW. A learned correction SHOULD win where IDW
  fails -- the SPARSE regime, where the neighbour field is under-sampled. GOAL: trace
  MAE(ours) and MAE(IDW) vs network DENSITY on one curve, find the CROSSOVER where the
  correction flips from >=IDW (dense) to <IDW (sparse), and claim the sparse regime.

  MECHANISM (new, reusable): train.py SUBSAMPLE_N/SUBSAMPLE_SEED -- randomly thin to N
  sensors BEFORE knn_edges/IDW are built, so the graph + IDW field are genuinely lower
  density (not just fewer targets). eval_inductive flags --max-nodes / --subsample-seed.
  Verified: --max-nodes 10 -> nodes=10 edges=60 split(7,1,2); --max-nodes 6 -> nodes=6.
  SWEEP (run_density_sweep.sh -> density_sweep_logs/results.csv; plot_density_crossover.py):
  densities N in {6,8,10,12,14,16,18,20,22}, 3 random subsets each (subsample-seed 0-2),
  8 split-seeds per point. TWO OURS configs vs the fixed 1/d^2 IDW baseline:
    krig = --kriging-prior --kriging-range 5000 --corr-reg 0.6  (worklog SPARSE-best: BLUP
           beats IDW when sparse; the correction has room at cr0.6)
    idw  = --idw-prior --idw-feature --corr-reg 0.3             (loosened residual on IDW)
  RATIONALE for kriging: the eval's IDW baseline is naive 1/d^2; kriging (exp-cov BLUP) is
  a strictly better interpolator when samples are sparse -> OURS(krig) SHOULD undercut the
  IDW floor at low N, and the learned correction adds on top. At high N kriging over-smooths
  (worklog) so OURS>=IDW there = the expected crossover shape.
  Early single points (noisy, N=6 has train=4/val=1/test=1): per-seed OURS both beats and
  loses to IDW -> high variance, need the subset+seed averaging. Sweep RUNNING (54 evals).
  All UNCOMMITTED. NEXT: aggregate curve, locate crossover, then (if found) tighten the
  config AT that density to maximise the margin OURS<IDW = the publishable result.

================================================================================
[2026-07-11  EDGE-FEATURE ABLATIONS (user directive): GraPhy wind formula + temp/AOD edge gates]
  User: pull GraPhy's EXACT wind edge-feature from Sec 3.2 (arxiv 2506.06917), compare
  line-for-line to ours, and if different implement THEIR version (not a reinterpretation)
  + re-run the convection ablation. Separately build EDGE-feature temp & AOD (analog of the
  elevation edge gate) and ablate each. Budget: 3 ablations on on-disk data; report null if
  none beats 3.05/3.02 and flag before more compute.

  --- WIND EDGE FEATURE: GraPhy Sec 3.2 vs ours (line-for-line) ---
  GraPhy 3.2 (Convection Module): edge feature e_{j,i} = [w_v, w_A, dist_{i,j}]:
    w_v  = wind SPEED (magnitude)
    w_A  = cosine of the angle between wind direction and the sensor-PAIR bearing =
           cos(theta_wind - theta_ij), a STANDALONE directional similarity in [-1,1]
    dist = inter-sensor distance
  OURS (train.py + convection.py): edge_attr = [dist, wind_along, wind_speed] where
    wind_along = wind_speed * cos(theta_wind - theta_bearing)  (speed-SCALED along-component)
  VERDICT: a genuine VARIANT, differing in the DIRECTIONAL term. Same 3 ingredients, same
  wind-bearing angle, BUT we fed the PRODUCT speed*cos(Δ) and never the bare cosine; GraPhy
  keeps w_v and w_A=cos(Δ) SEPARATE so the edge-MLP learns their combination (strictly more
  expressive: [speed,cos] can represent speed*cos and more; ours can't recover bare cos w/o
  dividing out speed). Not identical -> implemented GraPhy's exactly: w_A = wind_along/|wind|
  = cos(Δ), edge feature [dist, w_A, w_v] (train.py, replacing the old wind_along term).

  --- RE-RAN run_convection_fresno.sh with GraPhy's wind (data/fresno, HRRR, 8 seeds, IDW 4.159) ---
    config      our-wind(prior)   GraPhy-wind(now)
    off_06 ctrl   4.130            4.130   <- BEST (control has no conv; wind term irrelevant)
    on_06         4.181            4.240
    on_03         4.296            4.151
    on_01         4.478            4.551
  RESULT: GraPhy's EXACT wind feature does NOT unlock convection. Best conv variant on_03
  4.151 is +0.02 over the no-conv control 4.130 = within noise (±0.83), still >= IDW 4.159.
  => The convection null was the DATA (common-mode HRRR wind, worklog-measured 17% spatial
  variation), NOT our wind-feature parameterization. Swapping to GraPhy's construction
  changes nothing material. Rules out "our wind feature was wrong" as the reason convection
  never helped. (Fresno data/fresno IDW 4.16, not the 3.05 dense best -- convection can only
  be tested where there's real wind = sparse data/fresno; the dense best has zero wind.)

  --- TEMP-EDGE + AOD-EDGE gates: built + RUNNING on Pittsburgh ---
  temp & AOD have NO Fresno data on disk (both Pittsburgh-only: aod_csv + temp_dir); user
  chose to run these two on Pittsburgh (has 58 usable sensors, real temp, 1km MAIAC AOD).
  Built the sensor-pair analog of the elevation edge gate (model.py): per-edge gate from the
  signed Δtemp / Δaod (dst-src), same exp form as ElevationGate, dampens diffusion edge
  weight + convection message. Time-varying (recomputed per hour). eval flags --temp-edge-gate
  / --aod-edge-gate precompute [T,E] signed diffs from temp_wide/aod_wide (missing->0->gate=1).
  Ablation run_pgh_edge_gates.sh: strong base (idw-prior, cr0.6, despike+spatial-qa, no-conv,
  8 seeds) vs +temp-edge vs +aod-edge. RUNNING. All UNCOMMITTED.

  --- PITTSBURGH TEMP-EDGE + AOD-EDGE GATE ABLATION (8 seeds, IDW 5.027) -> BOTH NULL ---
    config       OURS            vs base
    base         5.014 ± 0.343    --      (OURS ~ IDW 5.027, ties as on Fresno)
    +temp-edge   5.047 ± 0.379    +0.033  (slightly WORSE)
    +aod-edge    5.037 ± 0.348    +0.023  (slightly WORSE)
  Per-seed the gated runs differ slightly from base (e.g. seed3 base 4.556 / temp 4.437 /
  aod 4.487), so the gates ARE active (wired + modulating), just add NO signal -> both are
  a small net drag (extra params, zero payoff). SAME verdict as the NODE forms (temp gate
  null, AOD feature neutral): temp & AOD are COMMON-MODE in space, so the sensor-PAIR Δ is
  ~0 -> gate ~ 1 -> inert. The EDGE reformulation does not rescue them.

  ================= EDGE-FEATURE PASS VERDICT: 3/3 NULL, BUDGET STOP =================
  Per user budget ("if none of the three beats the base, stop + report null + flag before
  more compute"):
    1. WIND edge (GraPhy Sec 3.2 EXACT: [w_v, w_A=cos(Δ), dist], not our speed*cos): convection
       still ties the no-conv control (4.151 vs 4.130), still >= IDW 4.159. NULL. The wind
       null is the DATA (common-mode HRRR), not the feature form.
    2. TEMP edge gate (Pittsburgh): 5.047 vs base 5.014. NULL.
    3. AOD edge gate (Pittsburgh): 5.037 vs base 5.014. NULL.
  None beats its base; none approaches the Fresno best 3.05/3.02. STOPPING here on edge
  features rather than iterating on variants (h_init, signed-vs-abs, gate-vs-feature, conv-on,
  looser corr-reg) -- flagged back to user before spending more. The covariate story is now
  triple-confirmed (node form, then edge form, across wind/temp/AOD): metro-scale covariates
  are common-mode and cannot add SPATIAL discrimination the PM field lacks -- independent of
  whether they enter as node features, node gates, or edge gates. Reusable infra kept:
  train.py GraPhy wind feature; model.py temp_edge_gate/aod_edge_gate; eval --temp-edge-gate/
  --aod-edge-gate; run_pgh_edge_gates.sh. All UNCOMMITTED.

  --- DENSITY-CROSSOVER SWEEP: progress + partial finding (krig config) ---
  Runtime note (why slow): the KRIG config does a kriging BLUP linear solve PER HOUR
  (2951) x 8 seeds per eval = ~24k solves/point in Python -> ~1-2 min/point. The first
  ~12 points also shared 8 cores with the 5-way wind ablation + 3-way Pittsburgh ablation
  (both heavy) so it crawled at 6-12/54; once those finished it sped up (12->18 in minutes).
  Back half = the IDW config (plain 1/d, no BLUP) is much faster.
  PARTIAL (krig config, mean over 3 subsets; IDW baseline = fixed 1/d^2):
    N     IDW    OURS-krig    gap(OURS-IDW)
    6    4.203    4.995        +0.79
    8    5.122    5.596        +0.47
   10    3.692    5.195        +1.50
   12    3.192    3.401        +0.21
   14    ~        ~            (in results.csv)
   16    ~        ~
  FINDING (so far, NO crossover from kriging): OURS-krig is WORSE than IDW at EVERY density,
  and WORST when sparse (N=10 gap +1.50) -- the OPPOSITE of the crossover hypothesis. Cause:
  the fixed kriging range (5000 m) OVER-SMOOTHS a sparse net (few, far points -> BLUP blurs
  toward the mean), so it underperforms naive 1/d IDW exactly where we hoped it'd win. The
  gap SHRINKS as density rises (0.79->0.21 by N=12) -> converges toward IDW when dense. So
  kriging is not the sparse-regime winner; a fixed-range BLUP is a poor sparse interpolator.
  Waiting on the IDW config (looser 1/d residual, cr0.3) -- expected to track IDW closely
  (residual-on-IDW can at best tie its own prior). Full curve + plot on completion. UNCOMMITTED.

  --- DENSITY-CROSSOVER SWEEP: COMPLETE (52-54/54) -> NO ROBUST CROSSOVER ---
  Full curve (mean over 3 subsets; plot density_sweep_logs/density_crossover.png):
    N     IDW    OURS-krig   OURS-idw
    6    4.203    4.995      4.603
    8    5.122    5.596      5.476     (N=8 bump = subset variance: tiny-N draws hit hotspots)
   10    3.692    5.195      4.177
   12    3.192    3.401      3.368
   14    3.359    3.888      3.349 *   (idw barely under IDW, -0.010 = noise)
   16    3.459    3.837      3.334 *   (idw under IDW -0.125, isolated, within +-0.3-0.5)
   18    2.997    3.037      3.028
   20    2.993    3.300      3.210
   22    3.021    3.107      3.164
  VERDICT: the crossover hypothesis (learned correction BEATS IDW in the sparse regime) is
  FALSE for this architecture. Across ALL densities:
   * OURS-krig (kriging prior) is WORSE than IDW everywhere, worst when sparse (fixed 5000m
     range over-smooths a sparse net); converges toward IDW only when dense.
   * OURS-idw (loose 1/d residual, cr0.3) TRACKS IDW -- ties it, dips barely under at just
     N=14/16 (well within the +-0.3-0.5 subset noise), and is >= IDW at every other N.
   * NO density gives a ROBUST OURS < IDW. All curves sit far above GraPhy 2.38.
  MECHANISM: the predictor is IDW_prior + a corr-reg'd residual. When DENSE the residual has
  no signal to add (field well-sampled) -> ties IDW. When SPARSE the residual OVERFITS the
  few visible points -> adds NOISE -> drags BELOW IDW (worse). So sparsity makes the learned
  part HURT, not help -- the opposite of the hoped-for sparse-regime win. This is the 5th
  independent confirmation that residual-on-a-fixed-prior cannot beat IDW at any density.
  PUBLISHABILITY IMPLICATION (honest): to BEAT IDW (GraPhy's headline claim) you need a
  genuinely LEARNED interpolator -- learned/anisotropic adjacency WEIGHTS (GraPhy's actual
  diffusion; or GAT/KCN attention over neighbour VALUES), trained end-to-end -- NOT a
  residual bolted onto a fixed isotropic prior. Our scaffold structurally cannot win; the
  crossover, if one exists, lives in a different architecture. New files: run_density_sweep.sh,
  scripts/plot_density_crossover.py, scripts/extract_density_results.py, train.py SUBSAMPLE_N,
  eval --max-nodes/--subsample-seed. All UNCOMMITTED.

  ================= SESSION CONSOLIDATION (2026-07-11..12) =================
  Goal: push past the 3.05/3.02 plateau toward GraPhy 2.38 + find a publishable win.
  What was tried and KILLED this session (all rigorously, all documented above):
   1. Density re-fetch (12 empty sensors)  -> DEAD: no data in the Oct23-Jan24 window.
   2. Single-channel A/B recovery           -> DEAD: recovered sensors add variance/hurt.
   3. GraPhy-exact WIND edge feature         -> NULL: convection still ties no-conv control.
   4. TEMP-edge gate (Pittsburgh)            -> NULL: common-mode, pair-Δ≈0.
   5. AOD-edge gate (Pittsburgh)             -> NULL: common-mode, pair-Δ≈0.
   6. Density-crossover sweep                -> NO CROSSOVER: OURS >= IDW at every density.
  BEST RESULT UNCHANGED: fresno_dense_abc, OURS 3.04-3.05 / IDW 3.02, 1.27x off 2.38.
  THE TAKEAWAY: the residual-on-IDW architecture is at its ceiling (ties IDW, can't beat it);
  metro covariates are common-mode in node/gate/edge form alike; density can't be added for
  this window. The ONE remaining path to actually beat IDW = rebuild as a learned-weight
  interpolator (see MEMORY [[graphy-faithful-repro-task]]). Flagged to user.
================================================================================
[2026-07-11  FAITHFUL GraPhy REBUILD -- test the PUBLISHED architecture as-is]
================================================================================
GOAL (task): implement GraPhy (arXiv 2506.06917) EXACTLY as published -- three
parallel physics GNN modules (Diffusion Laplacian high-pass, Convection wind
message-passing, Local source/sink) combined by a DYNAMIC SOFTMAX FUSION -- as a
clean STANDALONE model, with NONE of this repo's IDW-residual/kriging/gate
scaffold. Train on fresno_dense_abc, report MAE vs current best (3.054) and
GraPhy's 2.38. This tests whether the real architecture (esp. the softmax fusion,
which the IDW-residual model never had) reaches the paper's number.

SETUP / PROVENANCE
  * Worktree branch gotcha: the implement-graphy worktree was based on an OLD
    commit (15f07bd, no Fresno/harness). Reset it onto branch `graphy` (a1ff1af)
    which carries the Fresno data + eval_inductive harness + WORKLOG. (memory:
    graphy-faithful-repro-task)
  * NEW FILES: src/model/graphy_faithful.py (the standalone model),
    scripts/eval_graphy_faithful.py (train+inductive eval, reuses
    build_static_graph + eval_inductive's split_nodes/idw_baseline/metrics).
  * build_graph2: added wind_hrrr_dir to fresno_dense_abc (points at
    data/fresno/wind_hrrr; dense_abc reuses those sensor ids) so the FAITHFUL
    convection module gets real HRRR wind. (IDW-residual path used --wind zero
    because convection was inert there.)

FAITHFUL CHOICES (differ deliberately from eval_inductive.py):
  * Architecture: 3 modules + per-node softmax fusion IS the layer update (no
    outer h+fused residual, no gates, no IDW prior/correction-reg).
  * Masking: exactly ONE train sensor masked per training example (paper), NOT
    IGNNK random-fraction.
  * Loss: plain MSE in raw ug/m3 (linear z-score for input conditioning only,
    inverted linearly before the loss). NO log/sqrt, NO Huber, NO grad clip.
  * Optim: Adam lr 1e-4 betas (0.9,0.999), batch 32. Configs: large 512/5-layer,
    small 128/3-layer.
  * Data: fresno_dense_abc + despike + spatial-qa (same DATA QA as the current
    best, so the ONLY difference vs best is the architecture). -> 24 nodes,
    2951 hrs, split (train/val/test)=(17/2/5).
  * Sub-module ambiguities flagged in-code (sigma=sigmoid; lambda_max computed
    once per static graph; local "graph conv" = degree-normalised then X=M.F with
    M=I+D; convection ResMessage adds edge feat to node feat).

RESULTS SO FAR (single-seed probes; plain MSE STABLE, no divergence):
    30 steps   -> MAE 13.7  (untrained, near-constant)
    800 steps  -> MAE 7.61  R2 0.576  corr 0.855   fusion w=(D .84, C .01, L .15)
  IDW baseline on same split: MAE ~3.01. Model still learning but SLOW at lr 1e-4;
  running 5000-step small + large-config probes to find the plateau. NB WORKLOG
  earlier (line ~111) noted the pre-scaffold GraPhyNet "plateaus ~6.3-6.6" -- so a
  faithful rebuild landing well above IDW would corroborate that the ARCHITECTURE
  (not just our old code) loses to IDW on this sparse 24-sensor network. TBD.

--- [2026-07-12] FAITHFUL GraPhy: the gap is GENERALISATION, fixed by val early-stop ---
Diagnostic curve (small, 10k steps, single seed) told the story:
  train-MSE(ug^2): step0 7142 -> 1000:65 -> 2000:26 -> 3000:17 then BOUNCES 45/52/22/16/32
  i.e. the one-sensor masking TASK converges (train RMSE ~4-6 ug) by ~2k steps, but
  held-out TEST MAE was 9.36 and got WORSE with more steps (800:8.26 -> 10k:9.36).
  => classic OVERFIT to the 17 training sensors; not an optimisation failure.
FIX (faithful, leak-safe): select the model by MAE on the reserved VAL sensors (disjoint
  from test) over a random 300-400h subset; keep best state; patience early-stop.
  This is standard train/val/test model selection, closer to how GraPhy actually trains
  than a fixed step count.
RESULT (small, 1 seed, best@4800): TEST MAE 9.36 -> 4.80  (R2 .83, corr .925), IDW 3.01.
  Plain MSE in raw ug/m3 STILL fully stable (no divergence) -- the old sqrt/huber/clip
  were NOT needed for the faithful architecture. Fusion w=(D .82, C .04, L .14):
  diffusion dominates, convection ~inert (matches every prior wind-null result here).
STATUS: faithful GraPhy, fairly trained, ~4.8 MAE -> ABOVE IDW 3.01 and repo-best 3.05
  (1.6x IDW). Corroborates the standing conclusion: on this 24-sensor network the
  published architecture does not beat IDW; the gap to GraPhy's 2.38 is DATA (their 41
  sensors + EPA-corrected PurpleAir), not the model wrapper. Launching full 8-seed
  small + large (512/5) runs, per-seed parallel, val early-stop.

--- [2026-07-12] FAITHFUL GraPhy SMALL config, 8-seed result ---
  FAITHFUL GraPhy  MAE 7.53 +- 2.56   R2 0.541 +- 0.26   RMSE 10.19
  IDW baseline     MAE 3.99 +- 0.58   (same 8 splits; repo-best 3.05, GraPhy 2.38)
  Per-seed MAE 4.59..13.65 (seed3 failed: corr 0.13). 0/8 diverged (plain MSE stable).
  Mean fusion w=(D .33, C .19, L .48) but per-seed weights swing wildly
  (diffusion-, local-, or mixed-dominant) -> the dynamic softmax fusion does NOT
  settle on a consistent module; NO single module reliably dominates, and convection
  is usually-but-not-always small. => faithful GraPhy is ~1.9x IDW here and
  split-sensitive; confirms the arch (not our old wrapper) loses to IDW on this
  24-sensor net. Running large (512/5) for the paper's small-vs-large comparison.

--- [2026-07-12] LARGE run STALLED on memory thrash -> rerun 4-way ---
ROOT CAUSE: launched all 8 hidden-512/5-layer seeds in parallel (1 thread each).
  Each large process holds ~1.5-2 GB (512-dim autograd graph over 768 block-diag
  nodes x 5 layers + libs). 8 x ~1.8 GB > 16 GB RAM -> 10.4 GB swap used, ~45 MB
  free -> heavy paging -> CPU accrual collapsed to ~1 min per 50 min wall (stall,
  not a hang; 0-byte logs were just Python block-buffering, not a crash). Small
  (hidden 128) was fine 8-way -- this is large-only.
FIX: run_graphy_faithful.sh now runs in WAVES of MAXPAR (default 4) concurrent
  seeds (2 threads each -> 8 threads on 10 cores, ~6-7 GB resident). Relaunching
  large at 4000 steps (small's best-val checkpoints were all <=4800; patience 8
  stops most earlier) so the run is bounded.

--- [2026-07-12] FAITHFUL GraPhy LARGE config (512/5), 4-seed result ---
  FAITHFUL GraPhy  MAE 6.51 +- 0.73   R2 0.70 +- 0.06   RMSE 8.68   (seeds 0-3)
  IDW baseline     MAE 3.71 +- 0.60   (same 4 splits)
  Per-seed MAE 5.56 / 6.24 / 6.65 / 7.58. 0/4 diverged.  Fusion w=(D .60, C .16, L .25).
  Only 4 seeds run (wave-2 skipped, user call) after the 8-way large run thrashed swap;
  rerun 4-way in waves. NB macOS bash 3.2 has no `wait -n` -> initial wave scheduler
  aborted under set -e after launching only seeds 0-3; runner fixed to portable group-wait.
COMPARISON (large vs small on the SAME seeds 0-3):
  small MAE: 7.89 / 6.95 / 6.06 / 13.65 (mean 8.64, seed3 catastrophic)
  large MAE: 5.56 / 6.24 / 6.65 / 7.58  (mean 6.51, no failures)
  => LARGE is better AND more robust (no blown seed), ~10-25% lower MAE, matching the
  paper's "large ~10% better than small" -- but STILL loses to IDW (3.71) by ~1.75x.
BOTTOM LINE: faithful GraPhy (published arch, dynamic softmax fusion, plain MSE in ug/m3,
  1-sensor masking, Adam 1e-4) trains STABLY (0/20 seeds diverged; no sqrt/huber/clip
  needed) and learns real spatial signal (corr .8-.93) but does NOT beat IDW on this
  24-sensor Fresno net: small 7.53+-2.56, large 6.51+-0.73 vs IDW ~3.7-4.0, repo-best
  3.05, GraPhy 2.38. Independently reproduces the standing conclusion that the gap to
  2.38 is DATA (41 denser + EPA-corrected sensors), not the model architecture/wrapper.
  Fusion never settles on one module; convection ~inert (wind redundant), diffusion &
  local share the load, mix varies by split.

--- [2026-07-12] EPA x DENSITY matrix on FAITHFUL GraPhy (small, 5 seeds) ---
Testing the two hypothesised gap drivers to GraPhy's 2.38. Wave 1 (calibration):
  cell              FAITHFUL GraPhy      IDW(same splits)
  abc, no EPA       8.17 +- 2.99         3.81 +- 0.57
  abc, +EPA         4.74 +- 1.12         3.04 +- 0.22
  => EPA/Barkjohn correction cut OUR MAE -42% (8.17->4.74) and IDW -20% (3.81->3.04):
     raw PurpleAir over-reading was inflating BOTH. Our EPA'd IDW 3.04 == repo baseline.
     Confirms Piece-1 (absolute scale). Model still loses to IDW ~1.56x -> relative-skill
     gap is separate (density/faithfulness). (bash gotchas fixed: set -e $() TAG abort;
     set -u empty-array; both were dropping the EPA=0 cells / skipping aggregation.)

--- [2026-07-12] FULL 2x2 MATRIX (faithful GraPhy small, 5 seeds) -- gap to 2.38 decomposed ---
  cell            sensors   FAITHFUL GraPhy     IDW(same splits)
  abc,  noEPA       22       8.17 +- 2.99        3.81 +- 0.57
  abc,  +EPA        22       4.74 +- 1.12        3.04 +- 0.22   <- BEST, closest to 2.38
  dense,noEPA       29       7.08 +- 1.62        4.86 +- 0.36
  dense,+EPA        29       5.64 +- 2.19        3.42 +- 0.34
FINDINGS:
  * CALIBRATION (EPA/Barkjohn) is a big lever both ways: abc model 8.17->4.74 (-42%),
    IDW 3.81->3.04; dense model 7.08->5.64, IDW 4.86->3.42. Raw PurpleAir over-reading
    was inflating the absolute MAE. Our EPA'd IDW (3.04) == repo baseline & is close to
    GraPhy's 2.38 model number.
  * DENSITY via fresno_dense is CONFOUNDED: the +7 sensors are exactly the ones A/B QA
    removes (miscalibrated). They make IDW WORSE (3.04->3.42 with EPA; 3.81->4.86 raw)
    and, once data is EPA-clean, make the MODEL worse too (4.74->5.64). So we cannot buy
    density without quality from the data on hand -- a clean 41-good-sensor test needs
    sensors we don't have. (Raw-data model did improve 8.17->7.08 with density, but that
    edge vanishes once calibration is fixed.)
GAP-TO-2.38 DECOMPOSITION (empirical):
    raw faithful GraPhy        8.17
    + EPA calibration      ->  4.74   (calibration ~ -3.4 MAE; the single biggest lever)
    remaining 4.74 vs IDW 3.04 = model doesn't beat IDW on 22 sparse sensors (overfits
      17 train sensors); GraPhy DOES beat IDW at 41 GOOD sensors -> needs real density.
    remaining IDW 3.04 vs GraPhy 2.38 = network density/quality (their 41 vs our 22).
  CONCLUSION: the ~3x raw gap is ~half calibration (fixable, and fixed here to 4.74) and
  ~half genuine network density of GOOD sensors (not fixable with our data, not an
  architecture flaw). Faithful GraPhy is sound; 2.38 is inseparable from GraPhy's dataset.

--- [2026-07-12] WHY WE CAN'T MATCH GRAPHY'S 41 SENSORS + SLC elevation-gate test ---
SENSOR AVAILABILITY (from WORKLOG 2026-07-10 "MATCH GRAPHY DENSITY"): a 43-sensor dense
  Fresno bbox WAS identified (enough to match 41), but the PurpleAir history fetch is a
  PAID API -- it died at sensor 9/43 with HTTP 402 PaymentRequiredError (key out of
  points, balance -20216). User later topped up but 12 of the 33 original CSVs were empty
  from failed fetches. Net: after A/B + coverage QA only ~22-29 usable sensors survive.
  So it's a DATA-ACQUISITION (credits) limit, not that the sensors don't exist. Confirmed
  today: fresno_dense's extra sensors (vs abc) are the QA-rejected noisy ones, so they
  don't help. Matching 41 GOOD sensors needs a fresh paid fetch we can't do here.
SLC ELEV-GATE TEST: since we can't get Fresno's density, test the faithful arch WITH the
  elevation gate (from src/model/elevation.py, imported into graphy_faithful.py) on SLC,
  which HAS terrain (22 nodes, |Delev| mean 119m / max 401m across 142 edges -> gate
  genuinely active; Fresno is flat so the gate is inert there). Gate wiring mirrors
  model.py: per-layer ElevationGate dampens diffusion (rebuilt gated Laplacian) +
  convection (message); local + fusion ungated. Running SLC gate-off vs gate-on, small,
  5 seeds, --wind zero (no SLC wind). Prior IDW+corr result (memory gate-ablation): elev
  GATE ~ -2.7% on SLC (terrain-aware IDW KERNEL was the big lever at -13.5%). NEW low-risk
  design: ungated path is byte-identical (external static L_D); gate only rebuilds L_D
  when on. bash runner generalized: CITY/EPA/GATE/WIND env vars.

--- [2026-07-12] SLC ELEVATION-GATE RESULT (faithful GraPhy, small, 5 seeds, wind zero) ---
  cell               FAITHFUL GraPhy      IDW(same splits)   vs IDW
  SLC, gate OFF      5.49 +- 1.15         4.71 +- 1.07       1.16x
  SLC, gate ON       4.96 +- 0.94         4.71 +- 1.07       1.05x  <- near parity
  => elevation gate cut MAE -9.7% (5.49->4.96), raised R2 .53->.62, and TIGHTENED variance
     (1.15->0.94). 0/5 diverged both. Fusion shifted local up (.47->.55), diffusion down.
KEY CONTRAST WITH FRESNO: on flat Fresno the faithful model is ~1.9x IDW and the gate is
  inert (Delev~0). On SLC's terrain the faithful model is already only 1.16x IDW, and the
  gate closes almost all of it (1.05x) -- the physics-transport modules the gate modulates
  have real signal to exploit when there's actual topography/valley-pooling. Bigger gate
  win here (-9.7%) than the IDW+corr model saw on SLC (~-2.7%, memory gate-ablation),
  plausibly because faithful GraPhy leans on transport modules while the IDW+corr model's
  IDW prior already captured most spatial structure so the gate had less to add.
CONCLUSION: the faithful GraPhy architecture is SOUND -- competitive with IDW on terrain,
  and the elevation gate is a real, transferable lever there. Its Fresno underperformance
  is data (raw calibration + too few good sensors), not the model.

===============================================================================
SESSION 2026-07-12 (cont.): Path A density fetch + Track 1/2 model additions
===============================================================================
PATH A (match GraPhy's 41-sensor density on Fresno):
  * Re-fetched the tight box (36.90,-119.92,36.60,-119.58) with LIVE credits,
    --extra-fields --ab-channels --min-spacing-km 0 -> data/fresno_full. The 12
    sensors that were empty stubs after last session's HTTP-402 came back EMPTY
    AGAIN (0 rows) => they are genuinely dead for Oct'23-Jan'24, not fetch fails.
    Usable stays 31/43. Gain: a/b channels now in-file on ALL sensors.
  * A/B QA on the full set (apply_ab_qa.py --src fresno_full --ab fresno_full):
    34 kept / 9 dropped -> 22 usable -> 20 nodes after coverage. IDW floor 3.38.
  * WIDER box 37.00,-120.05,36.50,-119.45 (53 listed, 228 m alt spread) supplement
    fetch (--exclude-existing data/fresno_full -> only 10 NEW downloads) + copied
    the 43 existing CSVs in -> data/fresno_med. 35 usable; A/B QA -> 25 usable ->
    23 nodes. **IDW floor 2.833 +- 0.433** (best network yet; 1.19x off 2.38).
  * CONCLUSION (now with fully re-fetched a/b-QA'd data): 2.38 is BELOW this
    network's IDW floor -> unreachable by ANY interpolator here. Density, not the
    model, is the wall. GraPhy's 41 live packed sensors don't exist in this
    bbox/window; the wider box lowers the floor to 2.83 but adds foothill spread.
  * apply_ab_qa.py generalised: added --src/--ab (combined-file fetches read a/b
    from the same CSV). build_graph2 CITY_CONFIG += fresno_full, fresno_full_abc,
    fresno_med, fresno_med_abc (all wind_hrrr_dir=data/fresno/wind_hrrr).
  * Main run launched: CITY=fresno_med_abc EPA=1 WIND=hrrr large 4000, 8 seeds
    (waves of 4). SLOW (~1.5-2 hr/wave). [result pending at handoff]

TRACK 1 (make OUR GraPhy beat the paper via a better elevation gate) -- DONE+smoked:
  * src/model/elevation.py += TerrainGate (diffusion) and DrainageGate (convection).
    Both keep the exp ENVELOPE so g(0)=1 exactly (no self-damping, stays in (0,1])
    but the DECAY RATE is a learned MLP of the edge terrain:
      TerrainGate:  rate = softplus(MLP([Δ/s, |Δ|/s, sign]))  -> g=exp(-|Δ|/s*rate)
      DrainageGate: rate = softplus(MLP([Δ/s,|Δ|/s,sign, w_A, w_A*sign]))  (directional)
    w_A = cos(wind_dir-bearing): the w_A*sign(Δ) interaction lets downslope-downwind
    transport damp at a different rate than upslope => drainage modelled along wind.
  * graphy_faithful.py: use_elev_gate -> gate_mode {none|elev|terrain}; terrain mode
    uses the two new gates (diffusion via rebuilt gated L_D, convection via message
    gate). gate_ctx now carries per-edge wind alignment. NEW flag: --terrain-gate.
    Ungated path byte-identical; --elev-gate (old 2-scalar) preserved for A/B.

TRACK 2 (hybrid: bolt the FAITHFUL physics modules onto an IDW prior) -- DONE+smoked:
  * eval_graphy_faithful.py NEW flags --idw-prior [--idw-prior-elev --idw-prior-h].
    pred_z = idw_prior_z(t, visible) + faithful_GraPhy_correction. z-space IDW over
    visible observed nodes (masked target joins hidden per training example -> no
    leak). Structural floor: correction=0 recovers IDW exactly. --idw-prior-elev adds
    terrain kernel exp(-|Δelev|/h) to the prior (inert on flat, valley-aware on SLC).
    Smoke @3 steps: fresno OURS 3.50 / IDW 2.72 corr .942 (vs pure-faithful 12.7 /
    .035) -> the prior anchors it near the floor as designed; train the residual down.

NEXT: (a) read wave-1 result of the fresno_med_abc large run; (b) benchmark
  --terrain-gate on SLC (8 seeds) vs base + old --elev-gate; (c) benchmark
  --idw-prior (+/- elev, +/- terrain-gate) on fresno_med_abc AND slc vs IDW & GraPhy.

===============================================================================
SESSION 2026-07-12 (cont. 2): paper protocol check + SLC ablation + base-conv fix
===============================================================================
PAPER PROTOCOL VERIFIED (arXiv 2506.06917, ACM TOSN 2025 full text):
  * Task = EXACTLY our protocol: mask ONE sensor, recover it from the other N-1
    (inductive kriging). 41 sensors -> 28 train / 4 val / 9 test. Input = SINGLE
    hourly snapshot per node (memoryless), held-out node = zeros. Loss = plain MSE.
    => our repro is faithful on architecture AND protocol AND input. Not the gap.
  * THEIR numbers (Table 2, 0.27 sensors/mi^2): GraPhy 2.3797, IDW 3.0653, GeoLSTM 3.373.
    KEY: their IDW=3.07; OUR IDW on fresno_med_abc=2.83 (BETTER). So "2.38 absolute"
    was the WRONG yardstick (diff networks). GraPhy's real claim = beat its own IDW by
    ~22%. On our net that margin => ~2.2. Earlier "2.38 unreachable vs our IDW floor"
    was apples-to-oranges; corrected.
  * Calibration: they use custom local linear-regression to nearest reference station
    (NOT EPA/Barkjohn, which we use). Their worse IDW hints noisier/more-spread data.
  * WHY our pure faithful sucks on Fresno (6.6, 2/4 collapse): TRAINING failure, not
    task/arch. We train on ~16 sensors vs their 28 -> a 512x5 GNN overfits; collapsed
    seeds show best@0 (val never beat init). Fresno field is smooth (IDW already 2.83)
    so little structure beyond IDW to learn -> unanchored model fits noise. Fixable.

SLC ABLATION (8 seeds each, isolates the terrain contribution):
  * pure faithful (handoff)                         5.49   loses to IDW 4.71
  * plain hybrid (--idw-prior only)                 4.800  LOSES to IDW 4.518 (+6%)
  * FULL STACK (--idw-prior --idw-prior-elev --terrain-gate) 4.190  BEATS IDW 4.518 (-7.3%)
  => the terrain kernel + terrain gate (Track 1) deliver the ENTIRE win: 4.80->4.19
     (-12.7%) flips loss->win. IDW anchor alone does NOT beat IDW; terrain awareness does.
     0/8 diverged in both hybrids. Clean "our terrain methods beat interpolation where
     vanilla GraPhy can't" result. Fresno hybrid (flat) 3.32 vs IDW 2.83 = same story
     (no terrain signal -> residual adds variance).

BASE-CONVERGENCE FIX (in progress): added --weight-decay, --dropout, --hidden, --layers
  to eval_graphy_faithful.py (+ WD/DROPOUT/HID/LAY env in run_graphy_faithful.sh).
  scripts/sweep_base_conv.sh sweeps pure-faithful fresno_med_abc EPA over small/tiny +
  wd/dropout to get it to BEAT our IDW 2.83 (the true apples-to-apples GraPhy repro).
  [results pending -> faithful_logs/sweep_base_console.log]

===============================================================================
SESSION 2026-07-12 (cont. 3): base-model convergence investigation (beat OUR IDW)
===============================================================================
GOAL: reproduce GraPhy's HEADLINE apples-to-apples -> get the PURE faithful model
(no gate, no hybrid) to BEAT our own IDW, like the paper beats its IDW 3.07->2.38.
Diagnosis (from cont.2): pure model overfits/collapses on too few train sensors.

CODE ADDED (regularization + capacity + training-length knobs):
  * eval_graphy_faithful.py: --weight-decay, --dropout, --hidden, --layers (override
    config capacity). graphy_faithful.py: _mlp() takes dropout (adds nn.Dropout after
    the hidden ReLU; dropout=0 -> byte-identical); threaded through all 4 modules +
    GraPhyLayer + GraPhyFaithful. Ungated/no-reg path unchanged.
  * run_graphy_faithful.sh: env flags WD, DROPOUT, HID, LAY, PATIENCE, VALEVERY, LR
    (all fold into EXTRA + TAG). scripts/sweep_base_conv.sh, sweep_train_len.sh.

EXPERIMENT 1 -- regularization sweep on fresno_med_abc (23 nodes / 16 train, IDW 2.83):
  * large (wave-1, cont.2)          6.6   (2/4 collapse)   loses 2.3x
  * small baseline (128/3)          5.49 +- 2.39   loses 1.94x
  * small + wd1e-3                  5.31 +- 1.82   loses 1.88x
  => regularization gives DIMINISHING returns; capacity/reg is not the lever. High
     variance (+-1.8-2.4) = seed instability, not just overfit.

USER PUSH: "why not run on the EXPANDED set?" -> fresno_med_abc WAS the expanded (wider
  box) set, but strict A/B QA + 40% coverage strangled 35 usable -> 16 train. FIX:
  relaxed QA (apply_ab_qa --frac-max 1.0 --recover-channel) keeps intermittent-
  disagreement sensors + recovers 5 on their healthy channel (all corr-to-city .88-.96)
  -> data/fresno_med_max: 32 usable -> **30 nodes / 20 train, IDW 3.59**. Registered in
  CITY_CONFIG. KEY: this is the PAPER ANALOG -- their IDW 3.07 on 28 sensors is noisier &
  denser than our ultra-clean 2.83; med_max (3.59, ~30 nodes) matches the paper's regime.
  Two distinct Fresno targets now: med_abc (clean, lowest ABSOLUTE MAE) vs med_max (dense,
  the net for reproducing "model beats its own IDW ~22%").

EXPERIMENT 2 -- regularization sweep on fresno_med_max (30 nodes / 20 train, IDW 3.59):
  * small baseline                  4.98 +- 1.67   loses 1.38x
  * small + wd1e-3                  4.91 +- 1.62   loses 1.37x
  => MORE SENSORS closed the model-vs-IDW ratio (1.94x -> 1.38x, user's instinct right),
     but reg still doesn't cross. Stopped remaining reg configs (compounding evidence).

EXPERIMENT 3 -- training-length (DIFFERENT question: under-convergence?): the 4000-step
  best-val steps spread 250..3999 with several at 3750/3999 (STILL IMPROVING at the stop)
  -> patience-10 halts good seeds early. sweep_train_len.sh on fresno_med_max: (1) 15k
  steps, patience 30, val-every 500; (2) lr 5e-4, 8k steps. [RUNNING; 15k is slow --
  8-way oversubscribes 8 cores, ~35% CPU/proc -> ETA ~45-60 min for cfg1, ~1.5-2h total.]
  If longer training pulls the mean under IDW 3.59 -> reproduction found. If it also
  plateaus ~4.9 -> honest conclusion: our faithful repro underperforms the paper on
  unspecified training details; the IDW-prior hybrid is the real fix.

--- EXPERIMENT 3 RESULT (training length on fresno_med_max, IDW 3.59) ---
  * 4000 steps  (patience 10): 4.98 +- 1.67   ratio 1.39x
  * 15000 steps (patience 30): 4.311 +- 1.02  ratio 1.20x   <- longer training -13% MAE, -39% var
  best-val steps @15k: 1000 7000 8000 9500 11500 14500 14500 14999
  => UNDER-CONVERGENCE CONFIRMED: 4/8 seeds best-val at 14.5k-15k = STILL IMPROVING at the
     end. The pure model is under-trained, not over-parameterized. Gap to IDW closes
     monotonically (1.39x->1.20x) and has NOT plateaued -> more steps should cross IDW 3.59.
  ROOT CAUSE of "faithful GraPhy sucks on Fresno" = TWO compounding, now-confirmed causes:
    (1) too few train sensors (16->20 via relaxed QA fresno_med_max: 1.94x->1.39x)
    (2) severe under-convergence (4k+patience10 halts early; 15k->4.31, still climbing).
  NEXT: higher LR (5e-4, config 2 running) for faster convergence; then a long
  (25-30k) run to actually beat IDW. Use 4-way not 8-way (8-way load=21 on 8 cores,
  ~38% efficient -> 15k took ~140 min; 4-way would ~halve wall time).

===============================================================================
SESSION 2026-07-12 (cont. 4): FAITHFULNESS AUDIT vs paper equations + convection fix
===============================================================================
Re-read arXiv 2506.06917 module equations to make graphy_faithful.py the LITERAL arch
(free points before declaring any "faithful GraPhy" result). Findings:
  * Diffusion: paper l (.) sigma(L_D X W) AND "input features first undergo an MLP to
    learn node representations" -> our node_mlp->W->l IS faithful (earlier worry wrong).
  * Local: X=M.F, M_ii=1+sum e -> matches.
  * Fusion: 1 MLP -> softmax(3) -> matches.
  * Fresno hyperparams: 5 layers, hidden 512 -> our --config large. (Faithful Fresno
    comparison must use large, not small; small is a fast proxy.)
  * CONVECTION MESSAGE = THE FREE-POINTS GAP: paper ResMessage phi = MLP(concat[x_i+e,
    x_j+e]) -- BOTH endpoint node features, each + edge. We were feeding msg_mlp(h[src]+e)
    ONLY (source), dropping the target-node term + halving the input width. FIXED:
    msg_mlp now 2*dim in; m = msg_mlp(concat[h[dst]+e, h[src]+e]). (This is the module the
    user had previously RASHLY removed instead of fixing; now implemented properly.)
  * Ambiguous / NOT changed (paper silent, changing risks anti-faithfulness): whether the
    node MLP is ONE shared MLP feeding all 3 modules vs our per-module node_mlp; exact
    readout head (we use a Linear). Flagged, left as-is.
  Smoke: fixed convection compiles + runs; convection fusion weight active (~0.34).
  Validation run: 2 seeds small 4000 on fresno_med_max vs pre-fix 4.98 [pending].
